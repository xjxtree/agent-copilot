# AGENTS.md

This file is the shared project instruction entrypoint for coding agents such as Codex, Claude Code, Pi, opencode, and other compatible tools.

## Project State

- Current phase: **V2.71 Guided Cleanup safe-action deep links completed**。V2.21-V2.71 已收口同步：scan accuracy/dedupe、finding/conflict 语义、Health/Adapter Capability UX、single-skill Detail 诊断口径、Agent-config timeline、Finding explainability、Skill identity/provenance dedupe、Conflict semantic closeout、Finding triage persistence、AI skill analysis workflow、Cleanup Queue、Rule tuning / suppression、Safe batch actions、Cross-agent comparison view、Local report export、Pi writable evidence harness、Pi guarded writable toggle、Hermes explicit external roots、OpenClaw workspace deepening、Adapter diagnostics、AI Provider Foundation、Prompt Preview / Redaction、AI Skill Quality Score、AI Task Readiness Check、AI Routing Confidence、Task Benchmark Set、Routing Regression Detection、Agent Behavior Trace Import、Routing Accuracy Dashboard、Cross-agent Task Readiness、Stale / Drift Detection、Local Knowledge Index、Similar Skill Grouping、Capability Taxonomy、Workspace Readiness、AI Remediation Planner、Fix Preview Drafts、Impact Preview、Batch Review Workflow、Remediation History、Prompt Run History、Agent Session Skill Review、Local Skill Map、Provider Observability、Task-first Cockpit、Skill Lifecycle Timeline、Guided Cleanup Flow、Task Cockpit primary entry / Analysis IA、Privacy / Screenshot Mode + localization polish、Swift / Rust feature modularization、Guided Cleanup safe-action deep links 均已同步到主线。V2.71 adds service `safe_action_deep_link` / `deep_link` metadata and native safe-link buttons that route only to existing read-only or preview-first surfaces; it does not add apply/write/toggle/script/provider-confirm paths.
- V2.38 已完成；Hermes `skills.external_dirs` 仅建模为 explicit external roots，不推断 generic project roots。Hermes external-dir skills 以 read-only provenance 展示，toggle/install/writable/script execution/AI write-back/credentials 仍 blocked。
- V2.37 已完成；Pi guarded writable slice 基于 V2.36 disposable evidence 实现最小 native global/project/package toggle。切换要求 preview→确认→snapshot/rollback，并要求写入后触发禁用态重扫；project/package toggle 需要 trusted project settings。该切片**不支持** Pi install、脚本执行、LLM 自动写回、凭据持久化，且不对任意兼容 root 开放可写。
- V2.37 验证：focused Rust/Swift/protocol checks、`pnpm check:macos`、非 fixture real local app smoke launch/window capture、bundled `service.status` Pi capability check、`pnpm check:privacy` 与截图人工检查已通过。macOS Computer Use 工具本轮返回 `cgWindowNotFound`，但直接 CG/AX 证据能解析 `SkillsCopilot` 窗口；该项记录为工具层 blocker，后续 UI/service/protocol 变更仍需重跑真实交互验证。
- V2.28 completed 验收口径：`Conflicts` 仅保留当前/selected/current-agent 的 runtime/name 冲突；跨 agent 的 duplicate name / source overlap / enabled mismatch 只进入 `Analysis`；health 中 `conflict_count` 只能统计同-agent 冲突分组，不可叠加 cross-agent analysis 分组。
- Near-term priority: keep the product focused on AI-native skills management, inspection, analysis, task-centered skill review, local skill mapping, provider observability, task-first cockpit views, skill lifecycle understanding, guided cleanup, cockpit-first IA, screenshot-safe evidence, maintainable feature modules, and safe guided-cleanup navigation for AI agent applications, then continue consolidation through V2.72. Preserve V2.27 provenance/identity explainability, V2.28 conflict semantics, V2.29 finding triage persistence, V2.30 read-only AI skill analysis workflow, V2.31 Cleanup Queue, V2.32 local rule tuning/suppression, V2.33 safe batch actions, V2.34 cross-agent comparison, V2.35 local report export, V2.36 Pi writable evidence harness, V2.37 Pi guarded toggle, V2.38 Hermes external roots, V2.39 OpenClaw workspace deepening, V2.40 adapter diagnostics, V2.41 provider foundation, V2.42 prompt preview/redaction, V2.43 skill quality score, V2.44 task readiness, V2.45 routing confidence, V2.46 task benchmark set, V2.47 routing regression detection, V2.48 trace import, V2.49 routing accuracy dashboard, V2.50 cross-agent task readiness, V2.51 stale/drift detection, V2.52 local knowledge index, V2.53 similar skill grouping, V2.54 capability taxonomy, V2.55 workspace readiness, V2.56 read-only remediation planning, V2.57 read-only fix preview drafts, V2.58 read-only impact preview, V2.59 read-only batch review, V2.60 app-local remediation history, V2.61 app-local prompt run history, V2.62 app-local agent session skill review, V2.63 local skill map, V2.64 provider observability, V2.65 task-first cockpit, V2.66 skill lifecycle timeline, V2.67 guided cleanup, V2.68 cockpit-first IA, V2.69 privacy/screenshot mode, V2.70 module boundaries, and V2.71 safe-action links. Do not reintroduce full-platform UI adaptation, formal signed release/notarization/DMG/ZIP/public distribution, script execution, cloud sync, telemetry, or automatic write paths.
- Post-V2.71 planning boundary: V2.72 is planned, not implemented. V2.72 should harden validation with fixture/real screenshot matrix and Computer Use blocker taxonomy beyond the V2.69 lock/black capture rejection and V2.71 locked-session closeout.
- V2.43 completed boundary: AI Skill Quality Score is user-triggered + read-only, based on deterministic local evidence (`metadata` / `findings` / `conflicts` / `analysis` / `adapter diagnostics`). It does not write skill files, mutate agent config, create snapshots, change triage, execute scripts, read credentials, or send provider traffic from the scoring method. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.44 completed boundary: AI Task Readiness Check is user-triggered + read-only, based on deterministic local evidence (`metadata` / `findings` / `conflicts` / `analysis` / `adapter diagnostics` / V2.43 `quality_score`). It evaluates task text against local candidate skills, enabled/scope/risk state, gaps, blockers, evidence references, and safety flags. It does not write skill files, mutate agent config, create snapshots, change triage, execute scripts, read credentials, or send provider traffic from `task.checkReadiness`. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.45 completed boundary: AI Routing Confidence is user-triggered + read-only, based on deterministic local evidence (`task` text, metadata, findings, conflicts, cross-agent analysis, adapter diagnostics, V2.43 quality, and V2.44 readiness signals). It ranks route candidates with confidence, match reasons, ambiguity/collision warnings, likely wrong-pick risk, likely miss risk, evidence references, and safety flags. It does not write skill files, mutate agent config, create snapshots, change triage, execute scripts, read credentials, or send provider traffic from `task.rankSkillRoutes`. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.46 completed boundary: Task Benchmark Set is user-triggered + app-local. Benchmark definitions persist only under app data `task-benchmarks.json`; deterministic evaluation reuses V2.44 readiness and V2.45 routing evidence to compare top routes against expected skill refs/names and acceptable agent/scope constraints. `task.evaluateBenchmarks` does not send provider requests, write skill files, mutate agent config, create snapshots, change triage, execute scripts, read credentials, store raw prompt/response, sync cloud data, or emit telemetry. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.47 completed boundary: Routing Regression Detection compares V2.46 benchmark outputs against app-local baselines saved in `task-routing-baseline.json`. `task.saveRoutingBaseline` writes only app-local baseline metadata derived from deterministic benchmark evaluation. `task.detectRoutingRegression` is read-only and reports score/confidence drops, match-status worsening, top-route changes, new blockers/gaps, missing benchmarks, and missing catalog using local evidence only. No provider request participates in local regression scoring; no skill files, agent config, snapshots, triage, scripts, credentials, raw prompts/responses, cloud sync, or telemetry are touched.
- V2.48 completed boundary: Agent Behavior Trace Import is user-triggered + app-local. `trace.importLocal` accepts pasted/local transcript text (`content` / `trace_text` / `transcript`) plus optional task/agent/expected skill refs/names, redacts before persistence, and stores only redacted metadata/excerpt plus deterministic `analysis` in app data `trace-imports.json`. `trace.listImports` and `trace.deleteImport` only manage app-local trace metadata. No raw trace content, provider scoring request, credentials, skill files, agent config, snapshots, triage, scripts, raw prompts/responses, cloud sync, or telemetry are touched. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.49 completed boundary: Routing Accuracy Dashboard is user-triggered + read-only. `routing.accuracyDashboard` aggregates app-local V2.48 trace outcomes, V2.46 benchmark evaluation, and V2.47 regression evidence into summary counts/rates, per-agent rows, history rows, gap/issue rows, recent evidence rows, blocker notes, prompt request metadata, and safety flags. It writes no dashboard artifact, stores no raw trace, sends no provider request, reads no credentials, and does not mutate skill files, agent config, snapshots, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.50 completed boundary: Cross-agent Task Readiness is user-triggered + read-only. `task.compareAgentReadiness` compares the same task across locally visible supported agents and returns summary, per-agent rows, optional recommended agent, best candidate skill, comparison/readiness/routing scores, enabled/scope/risk state, benchmark and routing accuracy context, gap/issue rows, evidence references, prompt request metadata, and safety flags. It sends no provider request, writes no dashboard/comparison artifact, stores no raw trace, reads no credentials, and does not mutate skill files, agent config, snapshots, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.51 completed boundary: Stale / Drift Detection is user-triggered + read-only. `analysis.detectStaleDrift` derives stale/drift rows from deterministic local catalog evidence: fingerprint drift findings, current finding/conflict/analysis groups, source/root provenance, adapter diagnostics, catalog mtime staleness, readiness impact notes, gap/blocker notes, and evidence references. It sends no provider request, writes no stale/drift artifact, stores no raw trace, reads no credentials, and does not mutate skill files, agent config, snapshots, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.52 completed boundary: Local Knowledge Index is user-triggered + read-only. `knowledge.search` builds deterministic local search rows from existing catalog evidence and derived tags: purpose/description/body snippets, tools, keywords, rules, source/root provenance, agent/scope/state, risk tags, quality/readiness/stale-drift context, facets, gap/blocker notes, and evidence references. It sends no provider request, writes no index artifact, stores no raw trace, reads no credentials, and does not mutate skill files, agent config, snapshots, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.53 completed boundary: Similar Skill Grouping is user-triggered + read-only. `knowledge.groupSimilarSkills` groups duplicate, similar, confusable, source-overlap, and coverage-redundant skills from deterministic local evidence: canonical names, source/root provenance, V2.52 derived tags, shared tools/keywords/rules/capability/risk tags, quality/readiness/stale-drift context, member evidence refs, and gap/blocker notes. It sends no provider request, writes no grouping artifact, stores no raw trace, reads no credentials, and does not mutate skill files, agent config, snapshots, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.54 completed boundary: Capability Taxonomy is user-triggered + read-only. `knowledge.buildCapabilityTaxonomy` derives capability domains, coverage rows, representative skills, agent/workspace coverage, gaps, blockers, duplicate/redundancy signals, routing ambiguity signals, evidence refs, prompt preview metadata, and safety flags from deterministic local catalog evidence plus V2.52 knowledge tags, V2.53 similar groups, quality, stale/drift, findings, conflicts, analysis, adapter diagnostics, and source provenance. It sends no provider request, writes no taxonomy artifact, stores no raw trace, reads no credentials, and does not mutate skill files, agent config, snapshots, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.55 completed boundary: Workspace Readiness is user-triggered + read-only. `workspace.checkReadiness` derives workspace checklist rows, per-agent readiness, capability readiness, gap/blocker notes, evidence refs, prompt preview metadata, and safety flags from deterministic local catalog evidence plus V2.54 taxonomy, task readiness/routing signals, cross-agent readiness, stale/drift, findings, conflicts, analysis, adapter diagnostics, and source provenance. It sends no provider request, writes no readiness artifact, stores no raw trace, reads no credentials, and does not mutate skill files, agent config, snapshots, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.56 completed boundary: AI Remediation Planner is user-triggered + read-only. `remediation.plan` derives prioritized remediation plan items, priority rows, gap/blocker notes, evidence refs, prompt preview metadata, and safety flags from deterministic local catalog evidence plus findings, cleanup queue, stale/drift, similar grouping, V2.54 taxonomy, V2.55 workspace readiness, optional task readiness/routing, conflicts, analysis, adapter diagnostics, and source provenance. It sends no provider request, writes no remediation artifact, stores no raw trace, reads no credentials, and does not mutate skill files, agent config, snapshots, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider explanation remains previewed/redacted/confirmed and copy-only.
- V2.57 completed boundary: Fix Preview Drafts is user-triggered + read-only. `remediation.previewDrafts` derives copy/edit-ready frontmatter, description, permissions, dependency, and policy draft suggestions from deterministic local catalog evidence plus findings, skill metadata/frontmatter/body snippets, remediation plan signals, conflicts, analysis, rules, adapter diagnostics, writable capability context, and source provenance. It sends no provider request, writes no draft artifact, stores no raw trace, reads no credentials, exposes no direct apply/write path, and does not mutate skill files, agent config, snapshots, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider wording remains previewed/redacted/confirmed and copy-only.
- V2.58 completed boundary: Impact Preview is user-triggered + read-only. `remediation.previewImpact` derives impacted task, agent, skill, risk delta, and snapshot/rollback planning rows from deterministic local catalog evidence plus remediation plans/drafts, findings, conflicts, quality/readiness/routing/cross-agent/stale-drift/taxonomy/workspace evidence, adapter diagnostics, writable capability context, and source provenance. It sends no provider request, writes no impact artifact, stores no raw trace, reads no credentials, exposes no apply/write path, creates or rolls back no snapshot, and does not mutate skill files, agent config, triage, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider wording remains previewed/redacted/confirmed and copy-only.
- V2.59 completed boundary: Batch Review Workflow is user-triggered + read-only. `remediation.batchReview` derives local review groups/items from deterministic catalog evidence plus remediation plan/drafts/impact, cleanup queue, findings, conflicts, task context, adapter diagnostics, writable capability context, and source provenance. It groups candidate review work by task/risk/rule/agent/workspace and returns safe next-step labels, gap/blocker notes, evidence refs, prompt metadata, and safety flags. It sends no provider request, writes no batch artifact, stores no raw trace, reads no credentials, exposes no apply/write path, creates or rolls back no snapshot, does not mutate triage, and does not mutate skill files, agent config, scripts, cloud sync, telemetry, raw prompts, or raw responses. Optional provider wording remains previewed/redacted/confirmed and copy-only.
- V2.60 completed boundary: Remediation History is user-triggered + app-local metadata only. `remediation.recordHistory` stores redacted remediation decision metadata, source refs, recurrence/reopened markers, readiness/routing improvement notes, blocker/gap notes, evidence refs, and safety flags in app data `remediation-history.json`; `remediation.listHistory` and `remediation.deleteHistory` only read/delete that app-local history metadata. It sends no provider request, writes no skill files or agent config, mutates no triage, creates or rolls back no snapshot, executes no scripts, reads no credentials, persists no raw prompt/response/trace, syncs no cloud data, and emits no telemetry. Optional provider wording remains previewed/redacted/confirmed and copy-only.
- V2.61 completed boundary: AI Analysis UX / Prompt Run History keeps provider-backed analysis user-triggered, prompt-previewed, redacted, explicit-confirm, read-only, and copy-only. `llm.confirmPromptAndSend` may wait up to 10 minutes for the foreground provider response, then stores redacted prompt run metadata and extracted draft output in app data `prompt-runs.json`; `llm.listPromptRuns` only reads that local history. It does not persist raw prompt, raw provider response JSON, API keys, credentials, raw trace, unredacted local paths, skill files, agent config mutations, snapshots, triage mutations, scripts, cloud sync, or telemetry.
- V2.62 completed boundary: Agent Session Skill Review is user-triggered, deterministic, read-only, and app-local metadata only. `session.reviewAgentSkillUse` reviews pasted/imported agent sessions or referenced trace imports for hit/miss/wrong-pick/ambiguous/unknown outcomes, detected vs expected skills, duplicate/similar-skill interference, safe next steps, and evidence refs; `session.listSkillReviews` and `session.deleteSkillReview` only read/delete app-local `agent-session-reviews.json` metadata. It must not persist raw transcript, raw prompt, raw response, secrets, unredacted local paths, skill files, or agent config; it must not mutate triage, create or roll back snapshots, execute scripts, send provider requests, sync cloud data, or emit telemetry.
- V2.63 completed boundary: Local Skill Map is user-triggered, deterministic, read-only, and derived from existing local evidence only. `knowledge.buildLocalSkillMap` builds a navigable map over catalog skills, source/root provenance, knowledge tags, similar groups, capability taxonomy, same-agent conflicts, cross-agent analysis, task readiness/routing/session-review context, stale/drift and risk evidence. It returns map nodes, edges, clusters, coverage rows, gap/blocker notes, evidence refs, prompt metadata, and safety flags without creating a new source of truth or persisting a map artifact by default. It must not write skill files or agent config, mutate triage, create or roll back snapshots, execute scripts, read credentials, send provider requests by default, persist raw prompt/raw response/raw trace/secrets/unredacted paths, sync cloud data, or emit telemetry.
- V2.64 completed boundary: AI Provider Observability is user-triggered, deterministic/read-only, and derived from app-local evidence only. Method `llm.providerObservability` reads V2.61 prompt run metadata plus existing minimal provider call metadata and returns summary, call/history rows, provider/model/destination grouping rows, status rows, budget usage hints, retention recommendations, evidence refs, prompt metadata, and safety flags. It does not persist or return raw prompt, raw response JSON, API keys, credentials, raw traces, secrets, unredacted local paths, skill files, or agent config; it does not mutate triage, create or roll back snapshots, execute scripts, send provider/network requests by default, sync cloud data, or emit telemetry. Export/cleanup remains recommendations only in this slice; no deletion/export write control was added.
- V2.65 completed boundary: Task-first Cockpit is user-triggered, deterministic/read-only, and derived from existing local evidence only. Method `task.buildCockpit` reads task readiness, routing confidence, benchmark/regression, session review, provider observability, remediation plan/batch review, gap/blocker, and catalog evidence to return cockpit sections, task rows, agent route rows, skill candidate rows, readiness rows, context rows, evidence refs, prompt metadata, and safety flags. It does not create hidden task state, persist task artifacts, send provider requests, write skill files or agent config, mutate triage, create or roll back snapshots, execute scripts, read credentials, persist raw prompt/raw response/raw trace/secrets/unredacted local paths, sync cloud data, or emit telemetry. Optional provider wording remains V2.42-gated copy-only through prompt preview/confirmation.
- V2.66 boundary: Skill Lifecycle Timeline is user-triggered, deterministic/read-only, and derived from existing local evidence only. Method `skill.lifecycleTimeline` builds per-skill/per-agent/per-workspace lifecycle timeline rows from existing catalog evidence, scan/provenance/fingerprint state, stale/drift rows, finding/triage/remediation history, prompt run metadata, provider observability metadata, and session review outcomes where available. It must not persist new raw lifecycle artifacts by default, write skill files or agent config, mutate triage, create or roll back snapshots, execute scripts, send provider/network requests by default, read credentials, persist raw prompt/raw response/raw trace/secrets/unredacted local paths, sync cloud data, or emit telemetry.
- V2.67 completed boundary: Guided Cleanup Flow is user-triggered and evidence-only. Method `cleanup.planGuidedFlow` is deterministic/read-only and derives guidance only from existing local evidence: findings, cleanup queue, similar groups, stale/drift, readiness/routing/task cockpit, lifecycle timeline, remediation plan/drafts/impact/batch review, adapter diagnostics, and source provenance. Method `cleanup.recordGuidedStep` may write only app-local redacted guided cleanup step metadata such as `guided-cleanup-steps.json`. It must not write skill files or agent config, mutate triage, create or roll back snapshots, execute scripts, read credentials, send provider/network requests by default, persist raw prompt/raw response/raw trace/secrets/unredacted local paths, sync cloud data, or emit telemetry. Actual enable/disable/edit/remediation actions remain separate and must use existing preview-first / explicit-confirm safe methods.
- V2.68 completed boundary: Task Cockpit primary entry / Analysis IA is a user-visible SwiftUI navigation consolidation over existing local evidence. It makes Task Cockpit the default detail entry even when no skill is selected, moves Work surfaces ahead of diagnostic cards, changes the crowded segmented detail switcher into a bounded menu, and splits the former dense Analysis stack into Task Cockpit, Skill Map/Lifecycle, Guided Cleanup, Provider Observability, and Review surfaces. It must not add a service method, send provider/network requests by default, create hidden task state, write skill files or agent config, mutate triage, create or roll back snapshots, execute scripts, read credentials, persist raw prompt/raw response/raw trace/secrets/unredacted local paths, sync cloud data, or emit telemetry.
- V2.69 completed boundary: Privacy / Screenshot Mode + localization polish is UI/display and validation tooling only. It stores only an app-local screenshot privacy preference, redacts/collapses local paths in native UI presentation with explicit reveal-on-demand, propagates `app_language` for task cockpit/guided cleanup service rows, localizes Task Cockpit / Guided Cleanup / Provider Observability labels, verifies screenshot artifacts, and rejects locked/black/flat captures. It must not redact or mutate catalog facts, write skill files or agent config, create snapshots, mutate triage, execute scripts, read credentials, send provider/network requests by default, persist raw prompt/raw response/raw trace/secrets/unredacted local paths, sync cloud data, or emit telemetry.
- V2.70 completed boundary: Swift / Rust feature modularization is refactor-only. It extracts `TaskCockpitPanel.swift`, `DetailPresentationPrimitives.swift`, and Rust `cleanup_queue.rs`, and updates native UI layout verification to read split feature files. It must not change service method names, payload fields, protocol fixtures, scanner/catalog semantics, app-local persistence paths, provider behavior, write affordances, triage, snapshots, scripts, credentials, cloud sync, telemetry, or AI safety boundaries.
- V2.71 completed boundary: Guided Cleanup safe-action links are navigation-only over existing safe entries. `cleanup.planGuidedFlow` now returns `safe_action_deep_link` for each flow step and `deep_link` for each safe next action; native UI renders those as safe-link buttons and `SkillStore.openGuidedCleanupSafeLink` routes only to existing read-only or preview-first methods/sections (`cleanup.listQueue`, `remediation.plan`, `remediation.previewDrafts`, `remediation.previewImpact`, `remediation.batchReview`, `skill.lifecycleTimeline`, `task.buildCockpit`, `batch.previewSkillToggles` panel context, and `cleanup.recordGuidedStep`). It must not call apply/toggle/script/provider-confirm paths, write skill files or agent config, mutate triage, create or roll back snapshots, read credentials, persist raw prompts/responses/traces/secrets/unredacted local paths, sync cloud data, or emit telemetry. The safe batch link must not auto-preview or auto-apply; users still choose and confirm through the existing safe batch flow.
- V2.29 持久化边界：仅在 app-local triage state（catalog/app data）落盘，状态为 `Open` / `Reviewed` / `Ignored` / `Needs follow-up`；finding fingerprint 或受影响实例集合变化后，已持久化 triage 状态回到 `Open`；不写 agent config，不创建 skill-toggle snapshot 与 skill-content snapshot；不执行脚本，不做 AI 写回，不读/写凭据。
- V2.30 分析边界：`llm.prepareSkillAnalysis` 为用户显式触发、selected/batch 级别的 read-only preview；没有后台/周期性分析任务；未启用 provider 时保持本地 prepare/preview；provider 调用仅在未来显式 opt-in 模式下才应发生；AI 输出仅 draft/copy，不持久化 triage；不触发 agent-config/文件写入、snapshot 创建或 script execute。
- V2.41-V2.71 规划边界：AI 大模型成为复杂分析/判断的核心优势，但事实层仍由本地 deterministic scanner/rules/catalog 提供。V2.41 已完成 provider profile foundation：用户显式配置 endpoint / API key / model，优先支持 OpenAI-compatible 与 Claude-compatible 接口；API key 优先 Keychain，不能写入 SQLite、项目目录、日志、报告或 prompt artifact；仅 Test Connection 可显式联网并写入最小 redacted metadata。V2.42 起必须为真实分析调用提供 prompt preview/redaction/confirmation；V2.43 quality score、V2.44 task readiness、V2.45 routing confidence、V2.46 task benchmark evaluation、V2.47 routing regression detection、V2.48 trace import、V2.49 routing accuracy dashboard、V2.50 cross-agent task readiness、V2.51 stale/drift detection、V2.52 local knowledge index、V2.53 similar skill grouping、V2.54 capability taxonomy、V2.55 workspace readiness、V2.56 remediation planner、V2.57 fix preview drafts、V2.58 impact preview、V2.59 batch review、V2.60 remediation history、V2.61 prompt run history、V2.62 agent session skill review、V2.63 local skill map、V2.64 provider observability、V2.65 task-first cockpit 与 V2.66 skill lifecycle timeline 默认仅本地确定性评估，provider explanation 是可选的 V2.42-gated copy-only draft；V2.67 Guided Cleanup Flow 默认本地确定性评估，`cleanup.planGuidedFlow` 只读聚合 existing evidence，`cleanup.recordGuidedStep` 仅可写 app-local redacted metadata；V2.68 Task Cockpit primary entry 只是 UI/IA consolidation，不新增 AI/provider/write 语义；V2.69 Privacy / Screenshot Mode 只是 UI presentation / app-local preference / validation tooling，不改变 scanner/catalog 事实层；V2.70 只是 Swift/Rust module boundary refactor，不改变 service/capability 语义；V2.71 只是 guided cleanup navigation metadata / UI routing，不新增 apply/write/provider/execute 语义；所有 AI 分析必须 user-triggered、prompt-previewed、redacted、read-only by default，不能自动写回、自动执行或自动修改 agent config。
- V2.31 清理队列（Cleanup Queue）边界已完成：将 open findings、完整性问题与 cross-agent analysis insights 聚合为可复查列表；默认仅 read-only 显示，不新增自动清理、自动写入、自动执行或 provider 触发链路；可用的下一步动作仅指向已存在的安全入口（详情/过滤/复查/现有 toggle/save/rollback）。
- V2.32 Rule tuning / suppression 边界已完成：只允许 app-local rule severity override 与 suppression；必须可审计、可撤销；默认不影响 skill 文件或 agent config，不创建 skill-toggle / skill-content snapshot，不执行脚本，不调用 AI provider，不读写凭据，不触发快照写入。
- V2.33 Safe batch actions 边界已完成：只面向已 verified writable 的 agent/roots 提供预览优先（preview-first）的批量 enable/disable；必须先展示 agent/root 能力过滤、受影响 skill、不可写跳过项及跳过原因，并包含 snapshot/rollback 计划；Apply 必须经过显式确认且确认的 preview id 必须仍匹配当前 preview；Pi/Hermes/OpenClaw 保持 read-only；不新增 skill-content 写入、不执行脚本、不发起 AI provider 调用、不读写 credential，且不做 public distribution 任务。
- V2.34 Cross-agent comparison view 边界已完成：横向比较同名/相似 skills 在 Claude/Codex/opencode/Pi/Hermes/OpenClaw 中的状态、来源、风险、可写能力和差异；默认 read-only，不新增写入、执行、AI provider、credential 或 snapshot 路径。
- V2.35 Local report export 边界已完成：用户显式触发本地 Markdown/JSON 审计导出；输出写入 app data `report-exports`，报告内容递归脱敏 `$HOME`、`<project-root>`、`<project-cwd>`、`<app-data-dir>`；不做 public distribution、签名/notarization/DMG/ZIP、cloud sync、telemetry、provider 调用、credential 读写、脚本执行或自动写回。
- V2.11 scope: service/UI adapter capability matrix for Claude Code, Codex, opencode, Pi, Hermes, and OpenClaw, exposing scan/toggle/install status and blockers before new write affordances are enabled.
- Versioned adapter plan: V2.12 opencode writable evidence and guarded implementation complete; V2.13 Pi read-only scanner/parser complete with writable still blocked and direct root `.md` cataloging disabled due to real local noise; V2.14 Hermes and V2.15 OpenClaw evidence gates closed without implementation because maintainer-confirmed specs are missing.
- Completed management/analysis line: V2.16 OpenClaw read-only scanner, V2.17 Hermes read-only scanner, V2.18 cross-agent skill analysis, V2.19 skill health dashboard and triage UX, V2.20 read-only AI skill analysis assist, V2.21 scan accuracy/dedupe/agent metrics, V2.22 finding/conflict semantics, V2.23 Health Dashboard / Adapter Capability UX, V2.24 Skill Detail diagnostics, V2.25 Agent-config timeline, V2.26 Finding explainability, V2.27 Skill identity/provenance dedupe, V2.28 Conflict semantic closeout, V2.29 Finding triage persistence, V2.30 AI skill analysis workflow, V2.31 Cleanup Queue, V2.32 Rule tuning / suppression, V2.33 Safe batch actions, V2.34 Cross-agent comparison view, V2.35 Local report export, V2.36 Pi writable evidence harness, V2.37 Pi guarded writable toggle, V2.38 Hermes explicit external roots, V2.39 OpenClaw workspace deepening, V2.40 Adapter diagnostics, V2.41 AI Provider Foundation, V2.42 Prompt Preview / Redaction, V2.43 AI Skill Quality Score, V2.44 AI Task Readiness Check, V2.45 AI Routing Confidence, V2.46 Task Benchmark Set, V2.47 Routing Regression Detection, V2.48 Agent Behavior Trace Import, V2.49 Routing Accuracy Dashboard, V2.50 Cross-agent Task Readiness, V2.51 Stale / Drift Detection, V2.52 Local Knowledge Index, V2.53 Similar Skill Grouping, V2.54 Capability Taxonomy, V2.55 Workspace Readiness, V2.56 AI Remediation Planner, V2.57 Fix Preview Drafts, V2.58 Impact Preview, V2.59 Batch Review Workflow, V2.60 Remediation History, V2.61 Prompt Run History, V2.62 Agent Session Skill Review, V2.63 Local Skill Map, V2.64 AI Provider Observability, V2.65 Task-first Cockpit, V2.66 Skill Lifecycle Timeline, V2.67 Guided Cleanup Flow, V2.68 Task Cockpit primary entry / Analysis IA, V2.69 Privacy / Screenshot Mode + localization polish, V2.70 Swift / Rust feature modularization, and V2.71 Guided Cleanup safe-action links.
- Current mainline real local app validation status: V2.62 passes focused Rust/Swift/protocol checks, full service tests, `pnpm check:macos`, `pnpm check:privacy`, fixture screenshot inspection, and direct real local launch on 2026-06-12. The current `dist/SkillsCopilot.app` bundle was resolved by Computer Use, the app switched to the single-skill Analysis tab, and the Agent Session Skill Review panel was verified against real app data. This validation caught a Swift decode mismatch for service-wire session review records; the fix was added, the real UI then displayed outcome/detected/expected/reasons correctly, and the temporary app-local review metadata was deleted. No real-local screenshot was committed because the live UI exposes local paths. Future user-visible, UI, or service-protocol changes must rerun real local Computer Use validation; keep any new tool/window blocker explicit.
- V2.63 validation status: focused Rust/protocol checks for `knowledge.buildLocalSkillMap`, full service tests, focused/full Swift model and store checks, `pnpm check:macos`, `pnpm check:privacy`, `git diff --check`, and fixture smoke passed on 2026-06-12. Real local Computer Use validation reached the single-skill Analysis page, displayed Local Skill Map, clicked `Build Map`, and rendered live map output with nodes/edges/clusters plus evidence and safety sections; no live screenshot was committed because local paths are visible. A later coordinator exact-path Computer Use retry hit `cgWindowNotFound`/`remoteConnection` after duplicate same-bundle SkillsCopilot processes were present; this is recorded as a tool/window-layer blocker for that retry, not as a V2.63 product failure.
- V2.64 validation status: focused Rust/protocol checks for `llm.providerObservability`, full service tests, focused/full Swift decode/store checks, service protocol fixture decode, `cargo fmt --all -- --check`, `git diff --check`, `pnpm check:privacy`, and `pnpm check:macos` passed on 2026-06-12. The fixture macOS smoke launched `dist/SkillsCopilot.app`, captured a fixture window, and completed scan/toggle/settings/preview/rollback smoke coverage. Real local validation against the current bundle launched the app process, but System Events reported 0 windows after activation and clean relaunch, and Computer Use returned `cgWindowNotFound`; this is recorded as the V2.64 window/tool-layer blocker. No real-local screenshot was committed because live UI can expose local paths.
- V2.65 validation status: focused Rust/protocol checks for `task.buildCockpit`, full service tests, focused/full Swift model and store checks, service protocol fixture decode, `cargo fmt --all -- --check`, `git diff --check`, `pnpm check:privacy`, and `pnpm check:macos` passed on 2026-06-12. The fixture macOS smoke launched `dist/SkillsCopilot.app`, captured a fixture window, and completed scan/toggle/settings/preview/rollback smoke coverage. Real local validation launched the current bundle process from `dist/SkillsCopilot.app`, System Events confirmed the `SkillsCopilot` process, but System Events still reported 0 windows after activation and Computer Use returned `cgWindowNotFound`; this is recorded as the V2.65 window/tool-layer blocker. No real-local screenshot was committed because live UI can expose local paths.
- V2.66 validation status: focused Rust lifecycle/protocol checks, full service tests, focused/full Swift model and store checks, service protocol fixture decode, `cargo fmt --all -- --check`, `git diff --check`, `pnpm check:privacy`, and `pnpm check:macos` passed on 2026-06-12. The fixture macOS smoke launched `dist/SkillsCopilot.app`, captured a fixture window, and completed scan/toggle/settings/preview/rollback smoke coverage. Real local validation launched the current bundle process from `dist/SkillsCopilot.app`; the direct capture helper found an app window, but System Events reported 0 AX windows and Computer Use returned `cgWindowNotFound` for the absolute app path. This is recorded as the V2.66 window/tool-layer blocker. No real-local screenshot was committed because live UI can expose local paths.
- V2.67 validation status: focused Rust guided-cleanup/protocol checks, full service tests, focused/full Swift model and store checks, service protocol fixture decode, `cargo fmt --all -- --check`, `git diff --check`, `git diff --cached --check`, `pnpm check:privacy`, `pnpm check:macos`, and fixture screenshot inspection passed on 2026-06-12. The fixture macOS smoke launched `dist/SkillsCopilot.app`, captured a fixture window, and completed scan/toggle/settings/preview/rollback smoke coverage. Real local validation launched the current bundle process from `dist/SkillsCopilot.app`; direct capture found an app window, but System Events reported 0 AX windows and Computer Use returned `cgWindowNotFound` for the absolute app path. This is recorded as the V2.67 window/tool-layer blocker. No real-local screenshot was committed because live UI can expose local paths.
- V2.68 validation status: V2.68 multi-agent analysis, focused Rust checks for task cockpit/protocol/guided cleanup, full `swift test --package-path apps/macos`, native UI layout verification, full `pnpm check:macos`, fixture screenshot inspection, `pnpm check:privacy`, and `git diff --check` passed on 2026-06-13. The fixture macOS smoke launched `dist/SkillsCopilot.app`, captured a fixture window, and showed Work surfaces before adapter/health diagnostic cards with Task Cockpit selected by default. Real local validation launched the current bundle and CG window metadata found the `SkillsCopilot` window, but the macOS session was locked (`CGSSessionScreenIsLocked=Yes`), Computer Use timed out, and the final direct capture was black; this is recorded as the V2.68 locked-session/window-capture blocker. No real-local screenshot was committed.
- V2.69 validation status: V2.69 multi-agent analysis, `plutil -lint` for localizations, `pnpm test:macos-list-model`, `pnpm verify:macos-ui-layout`, `pnpm verify:screenshot-artifacts`, full `swift test --package-path apps/macos`, no-capture fixture smoke, `pnpm check:privacy`, and `git diff --check` passed on 2026-06-13. Full `pnpm check:macos` reached fixture capture and then failed closed with `locked-session: macOS session is locked; refusing to create screenshot evidence`, proving V2.69 no longer accepts locked screenshot evidence. No fresh V2.69 screenshot was committed. Real-local Computer Use returned `timeoutReached`, `ioreg` reported `CGSSessionScreenIsLocked=Yes`, and direct capture exited with `locked-session`. Real-local Computer Use should still be rerun when the macOS session is unlocked; locked-session, black-capture, flat-capture, Screen Recording permission, window-not-found, or Computer Use tool-layer failures must be recorded as blockers, not accepted visual completion evidence.
- V2.70 validation status: V2.70 multi-agent analysis, Rust cleanup queue module extraction checks, service protocol fixture/dispatch checks, full `swift test --package-path apps/macos`, `swift build --package-path apps/macos`, `pnpm verify:macos-ui-layout`, `pnpm verify:screenshot-artifacts`, no-capture fixture smoke, `pnpm check:privacy`, and `git diff --check` passed on 2026-06-13. Full `pnpm check:macos` passed build/test/service stages, then failed closed at fixture capture with `locked-session: macOS session is locked; refusing to create screenshot evidence`. Real-local Computer Use returned `timeoutReached`, `ioreg` reported `CGSSessionScreenIsLocked=Yes`, and direct capture exited 6 with `locked-session`; no fresh V2.70 screenshot was committed. Full real-local visual validation should still be rerun in an unlocked interactive session.
- V2.71 validation status: V2.71 multi-agent analysis, Rust guided-cleanup/protocol/dispatch checks, full `swift test --package-path apps/macos`, native layout verification, screenshot artifact verification, rebuilt no-capture fixture smoke, `pnpm check:privacy`, and `git diff --check` passed on 2026-06-13. Full `pnpm check:macos` passed build/test/service stages, then failed closed at fixture capture with `locked-session: macOS session is locked; refusing to create screenshot evidence`; real-local Computer Use returned `timeoutReached`. No fresh V2.71 screenshot was committed. Full real-local visual validation should still be rerun in an unlocked interactive session.
- V2.38 验证：focused Hermes adapter/scanner/commands tests、Swift tests、`pnpm check:macos` 已通过；真实 app smoke launcher 能启动当前 `dist/SkillsCopilot.app` 并取得窗口 id，但 Computer Use/AX/capture 本轮随后返回 `cgWindowNotFound` / 无可见窗口，作为工具/窗口层 blocker 保留；后续 V2.39 UI/service/protocol 变更仍需重跑真实交互验证。
- V2.39 验证：focused OpenClaw adapter/scanner/commands tests、list model、Swift tests、`pnpm check:macos` 已通过；真实 app smoke launcher 能启动当前 `dist/SkillsCopilot.app` 并取得窗口 id，但 Computer Use 返回 `cgWindowNotFound`，System Events 看到 0 个窗口，capture 无可见窗口，作为工具/窗口层 blocker 保留。
- V2.40 适配器诊断已完成：新增 read-only `adapter.listDiagnostics`、`service.status.adapter_diagnostics` / `app.stateSnapshot.status.adapter_diagnostics`，并在 sidebar Adapter Capabilities 展示各 agent 的 discovered / skipped / blocked roots、config 作用域、读写能力原因、最近一次 scan activity 与状态摘要。该阶段不新增写入、install、script execution、AI 自动写回、credentials 或 telemetry。
- Current implementation status after V2.71: code/docs now include provider profile configuration, Keychain-first API key storage, explicit `llm.testProviderConnection`, V2.42 `llm.previewPrompt` / `llm.confirmPromptAndSend`, redacted prompt previews, token/cost estimate, destination preview, confirmation-gated provider send, minimal redacted call metadata, V2.43 deterministic skill quality scoring, V2.44 deterministic task readiness, V2.45 deterministic routing confidence via `task.rankSkillRoutes`, V2.46 app-local task benchmark CRUD/evaluation via `task.listBenchmarks` / `task.saveBenchmark` / `task.deleteBenchmark` / `task.evaluateBenchmarks`, V2.47 routing regression baseline/detection via `task.saveRoutingBaseline` / `task.detectRoutingRegression`, V2.48 app-local trace import/list/delete via `trace.importLocal` / `trace.listImports` / `trace.deleteImport`, V2.49 routing accuracy dashboard via `routing.accuracyDashboard`, V2.50 cross-agent task readiness via `task.compareAgentReadiness`, V2.51 stale/drift detection via `analysis.detectStaleDrift`, V2.52 local knowledge search via `knowledge.search`, V2.53 similar grouping via `knowledge.groupSimilarSkills`, V2.54 capability taxonomy via `knowledge.buildCapabilityTaxonomy`, V2.55 workspace readiness via `workspace.checkReadiness`, V2.56 remediation planning via `remediation.plan`, V2.57 fix preview drafts via `remediation.previewDrafts`, V2.58 impact preview via `remediation.previewImpact`, V2.59 batch review via `remediation.batchReview`, V2.60 app-local remediation history via `remediation.listHistory` / `remediation.recordHistory` / `remediation.deleteHistory`, V2.61 app-local prompt run history via `llm.listPromptRuns`, V2.62 app-local deterministic Agent Session Skill Review via `session.reviewAgentSkillUse` / `session.listSkillReviews` / `session.deleteSkillReview`, V2.63 deterministic Local Skill Map via `knowledge.buildLocalSkillMap`, V2.64 deterministic/read-only Provider Observability via `llm.providerObservability`, V2.65 deterministic/read-only Task-first Cockpit via `task.buildCockpit`, V2.66 deterministic/read-only Skill Lifecycle Timeline via `skill.lifecycleTimeline`, V2.67 deterministic/read-only Guided Cleanup Flow via `cleanup.planGuidedFlow` plus app-local redacted guided cleanup step metadata via `cleanup.recordGuidedStep`, V2.68 cockpit-first native IA, V2.69 app-local screenshot privacy/path display governance, V2.70 module boundaries via Swift `TaskCockpitPanel` / `DetailPresentationPrimitives` plus Rust `cleanup_queue` module, and V2.71 guided cleanup safe-link metadata/UI routing. No V2.62-V2.71 implementation gap remains after the current closeout; V2.72 is planning/docs only until implemented, verified, and closed out.
- V2.8 rules/permissions governance, V2.9 Tool-global skill pool, and V2.10 docs/release consistency are integrated.
- Earlier integrated phases: MVP, Product UI/UX Hardening, V1 native macOS baseline, macOS Native Productization, V2 Prep security gates, refresh-summary UX, native test hardening, adapter evidence gates, first Codex adapter slice, and V2.1 through V2.7.
- The V2.2/V2.3 historical macOS/AX window-resolution blocker was resolved for the current mainline validation pass on 2026-06-10. Future user-visible, UI, or service-protocol changes must rerun real local Computer Use validation against the developer's real environment and keep any new blocker explicit; do not substitute smoke screenshots for real local validation.
- V2.8 covers LLM status protocol compatibility, permissions roundtrip, explicit severity ordering, findings filtering/grouping UI, and `app.stateSnapshot` refresh optimization.
- V2.8 completed local rules: `frontmatter.tools-not-empty`, `permissions.network-declared`, `permissions.exec-needs-human`, `name.canonical-case`, `script.no-shebang`, `body.too-long`, and `dependency.unknown`.
- V2.9 covers tool-global catalog/staging records, local directory import with rule audit, reproducible local export bundle/manifest, manifest reimport stability, native read-only preview UI, and confirmed install to Claude/Codex verified skill roots. GitHub clone import and script-file install are removed from the active product backlog. V2.12 added guarded opencode install to native roots after evidence verified writable semantics.
- V2.10 covers the documented skill execution safety boundary only: no real execution by default, local audit records for blocked/cancelled/failure attempts, and hard separation from LLM actions. Current product direction does not plan a script runner, sandbox runtime, public execution API, or successful execution output log.
- Near-term work prioritizes making every visible count and issue actionable and explainable before adding broader write support. Pi direct root `.md` cataloging is intentionally disabled after real local validation showed it pulls ordinary resource documents into the skills list. Pi writable remains limited to the V2.37 guarded native toggle slice; Pi install and compatibility-root writes remain blocked. Do not reopen V2.1-V2.25 for closed items unless a regression is found.
- OpenClaw project scope is workspace-scoped only: `<workspace>/skills` and `<workspace>/.agents/skills`; do not infer arbitrary repository roots as OpenClaw projects.
- Hermes has no confirmed generic project-level skills. First read-only scope is active/profile Hermes home `skills/**/SKILL.md`; explicit `skills.external_dirs` must be modeled as external roots, not automatic project roots.
- V2.3 Codex adapter hardening covers config patch robustness, adapter state expression, security regressions, smoke coverage, and documentation/status synchronization.
- V2.4 opencode originally introduced native-root scanning for `~/.config/opencode/skills` and active project `.opencode/skills`; current opencode scan also follows official `.claude/skills` and `.agents/skills` compatibility roots. V2.12 guarded writable support remains limited to managed `permission.skill` config overrides and native opencode install targets.
- The maintained product UI is the native macOS app in `apps/macos`, built with SwiftUI and AppKit interop.
- The Rust service protocol in `crates/service` is the UI boundary for the macOS app.
- The old Tauri/React UI and Tauri IPC shell have been removed. Do not recreate `ui/`, `src-tauri/`, or Tauri IPC for product work.
- The only current app bundle path is `dist/SkillsCopilot.app`.
- V2.7/V2.30 LLM local assist remains disabled by default until the user configures a provider. V2.41 adds provider profile settings, Keychain-first credentials, and explicit Test Connection; V2.42 adds prompt preview/redaction and confirmed provider-backed draft output; V2.43 adds deterministic read-only skill quality scoring; V2.44 adds deterministic read-only task readiness; V2.45 adds deterministic read-only routing confidence; V2.46 adds deterministic app-local task benchmark evaluation; V2.47 adds deterministic app-local routing regression detection; V2.48 adds deterministic app-local trace import/list/delete; V2.49 adds deterministic read-only routing accuracy aggregation; V2.50 adds deterministic read-only cross-agent task readiness comparison; V2.51 adds deterministic read-only stale/drift detection; V2.52 adds deterministic read-only local knowledge search; V2.53 adds deterministic read-only similar skill grouping; V2.54 adds deterministic read-only capability taxonomy; V2.55 adds deterministic read-only workspace readiness; V2.56 adds deterministic read-only remediation planning; V2.57 adds deterministic read-only copy/edit fix preview drafts via `remediation.previewDrafts`; V2.58 adds deterministic read-only impact preview via `remediation.previewImpact`; V2.59 adds deterministic read-only batch review via `remediation.batchReview`; V2.60 adds app-local remediation history via `remediation.listHistory` / `remediation.recordHistory` / `remediation.deleteHistory`; V2.61 adds app-local prompt run history via `llm.listPromptRuns`; V2.62 adds deterministic app-local Agent Session Skill Review; V2.63 adds deterministic read-only Local Skill Map via `knowledge.buildLocalSkillMap`; V2.64 adds deterministic/read-only Provider Observability via `llm.providerObservability`; V2.65 adds deterministic/read-only Task-first Cockpit via `task.buildCockpit`; V2.66 adds deterministic/read-only Skill Lifecycle Timeline via `skill.lifecycleTimeline`; V2.67 keeps guided cleanup planning read-only with app-local redacted step metadata only; V2.68 is cockpit-first UI/IA consolidation over those existing surfaces; V2.69 adds screenshot-safe UI presentation and validation tooling without changing AI/provider semantics; V2.70 splits modules without semantic change; V2.71 adds navigation-only guided cleanup safe links. V2.72 must preserve explicit opt-in, prompt preview/redaction for provider use, budget/observability, and no-write/no-execute guarantees.

## Architecture Rules

- Keep product logic in Rust workspace crates, not in the UI shell.
- `crates/core` is the no-I/O base layer. Higher crates may depend on it; it must not depend on higher crates.
- New UI work must call the typed Rust service protocol.
- Current implemented adapter scope is Claude Code, Codex, guarded writable opencode config/native install targets with native plus compatibility scan roots, guarded Pi native global/project/package toggles, and read-only Pi native directory skills under `SKILL.md`.
- Codex support is limited to verified user/project roots, cwd-to-repo-root project discovery, `catalog.scanAll`, project-context-scoped scanning, agent filtering/status display, and user-config writable toggles through `~/.codex/config.toml` / `$CODEX_HOME/config.toml`.
- Project-local Codex config writes, plugin/admin/system roots, Pi install and compatibility-root writes, Hermes writable/install, and OpenClaw writable/install remain blocked or read-only planning only per their evidence docs.
- Opencode writable support is limited to V2.12 verified exact managed `permission.skill` overrides and native opencode install targets; compatibility roots are scan-only sources.
- No cloud sync, accounts, telemetry, anonymous crash reports, or uncontrolled outbound network calls.
- Optional LLM features must be explicitly enabled by the user. V2.30 read-only analysis remains local prepare/preview and explicit action-triggered while no provider-backed analysis calls run by default. V2.41 provider settings support user-owned OpenAI-compatible and Claude-compatible endpoints without hardcoding a vendor. macOS credential storage must prefer Keychain, and fallback `~/.config/skills-copilot/llm.yaml` must be permission-checked as `0600` and contain only non-secret provider metadata unless the user explicitly accepts fallback secret storage. Credentials must never be written to SQLite, project directories, logs, prompts, response artifacts, screenshots, or reports.
- LLM output is untrusted. Draft frontmatter is display/copy-only in V2.7; do not add Apply/Write paths from LLM output. Real writes must go through the normal user edit/save flow and Rust service validation.
- Skill scripts are untrusted. V2.10 keeps script execution default-denied: LLM output, analyzer recommendations, imports, and tool-global previews must not trigger command execution. Do not add execution affordances while the product focus remains skill management, inspection, and analysis.

## Required Verification

- For code changes, run the focused checks needed for the touched area.
- For major changes, user-visible behavior, UI work, service protocol changes, or milestone completion, run `pnpm check:macos`, then run the real local app with `pnpm dev:macos` or `./script/build_and_run.sh run` and operate it with macOS Computer Use only after confirming the macOS session is unlocked and interactive.
- Smoke validation uses fixture data and must not touch the real user Claude config.
- Real local validation uses the developer's real local HOME, app data, and Claude config.
- Completed UI screenshots must capture only the full app window. Full desktop screenshots are forbidden.
- If the macOS session is locked, cannot be confirmed interactive, or Computer Use/window capture is blocked, state the blocker explicitly. Do not substitute a smoke screenshot for real local validation.
- Before committing, pushing, or handing off evidence, run `pnpm check:privacy`. Do not commit real local user paths, usernames, home directories, app-data paths, temp directories, credentials, tokens, proxy-managed credential placeholders, or screenshots that visibly expose those values. Use `$HOME`, `<repo>`, `<worktree>`, `<project-root>`, `<app-data-dir>`, and `<redacted>` placeholders in docs and fixtures. New screenshots require manual visual inspection because automated binary string scans do not perform OCR.

## Common Commands

```sh
cargo test --workspace
cargo clippy --workspace --all-targets --all-features
./script/build_and_run.sh --verify
pnpm build:macos
pnpm dev:macos
pnpm smoke:macos-app -- --fixture-data --capture-window
pnpm check:macos
pnpm check:privacy
pnpm capture:macos-window
swift test --package-path apps/macos
pnpm test:macos-list-model
pnpm benchmark:macos-list-model
pnpm verify:macos-ui-layout
```

## Read Before Editing

- Architecture changes: `docs/architecture.md`
- Agent workflow and verification rules: `docs/ai-agent-workflow.md`
- macOS app run and smoke rules: `docs/macos-app-runbook.md`
- UI prototype, screenshot, and verification standards: `docs/ui-delivery-standards.md`
- macOS native product plan: `docs/macos-native-plan.md`
- Data model: `docs/data-model.md`
- Security and privacy model: `docs/security-model.md`
- Roadmap and milestone status: `docs/roadmap.md`
- Current development task queue: `docs/development-tasks.md`
- Adapter scope and TBD specs: `docs/agent-adapters.md`
- Non-Claude adapter evidence gates: `docs/agent-adapter-spec-worklists.md`

## Git and Editing Rules

- Do not revert user changes unless explicitly asked.
- Keep edits scoped to the requested task and the relevant architecture boundary.
- Prefer existing project patterns over new abstractions.
- Update docs when behavior, commands, architecture, validation flow, or UI state changes.
- Before committing, check the working tree and only include intended changes.
- For multi-agent parallel work, create one isolated git worktree and branch per task before assigning subagents. Subagents must be told to work only in their assigned worktree, must not switch branches, and must not edit the coordinator checkout.

## V2.35 Local report export (completed)
- `V2.35` is completed after integration, `pnpm check:macos`, real local App export validation, and generated report redaction verification on 2026-06-10.
- Report export is user-triggered, local-only, and redacted: generated on demand in the app flow and written to local files under app data only after explicit user action.
- Local exports support Markdown and JSON audit artifacts covering agent coverage/status, health summary, open findings with persisted triage state, cleanup queue items, and cross-agent comparison insights.
- Exported artifacts redact local filesystem-sensitive values with placeholders such as `$HOME`, `<project-root>`, `<project-cwd>`, `<app-data-dir>`, and `<redacted>` before write.
- V2.35 remains explicitly out of scope for public distribution, DMG/ZIP packaging/signing/notarization, cloud sync, telemetry, provider/AI calls, credential storage, script execution, and automatic write-back.
- V2.33 Safe Batch constraints (explicit preview, explicit confirm, apply pre-checks for writable capability, snapshot/rollback plan, and apply-time preview id matching) stay unchanged and continue to be enforced.
- V2.34 Cross-agent comparison remains completed/read-only in scope and should not be converted to write operations as part of export work.

### V2.39 OpenClaw workspace deepening (completed)

- Scope is read-only workspace-scoped deepening only.
- Confirmed OpenClaw roots: `<workspace>/skills` and `<workspace>/.agents/skills`.
- Do not infer additional repository or parent roots for OpenClaw scans.
- Keep OpenClaw writable/install, credentials, and script execution blocked; no AI auto-write paths for this milestone.
- V2.39 is complete after implementation, focused validation, `pnpm check:macos`, and explicit real-app Computer Use/window blocker documentation.

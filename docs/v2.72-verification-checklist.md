# V2.72 Verification Checklist

V2.72 scope: validation harness hardening only. This version adds canonical blocker classification, lock-session preflight, screenshot artifact failure labels, fixture/real evidence rules, and documentation alignment. It does not change service protocol payloads, scanner/catalog semantics, provider behavior, write paths, snapshots, triage, script execution, credentials, cloud sync, or telemetry.

## Completed Checks

1. Multi-agent V2.72 review completed for capture scripts, screenshot artifact verification, documentation state, and validation command matrix.
2. `pnpm verify:validation-blockers` passed.
3. `pnpm classify:validation-blocker -- "Computer Use server error -10005: timeoutReached"` returned canonical `computer-use-timeout`.
4. Synthetic black PNG verification failed closed with canonical `black-capture`.
5. `pnpm verify:screenshot-artifacts` passed for committed PNG artifacts.
6. `pnpm smoke:macos-app -- --fixture-data` passed without writing real user config or requiring screenshot evidence.
7. `pnpm check:privacy` passed.
8. `git diff --check` passed.

## Locked-Session Result

The current macOS session is locked. Commands that require window capture fail closed instead of accepting invalid UI evidence:

- `pnpm smoke:macos-app -- --fixture-data --capture-window` fails with canonical `locked-session`.
- Full `pnpm check:macos` is expected to pass build/test/service stages and then fail closed at fixture capture with canonical `locked-session` until the session is unlocked.
- Real-local Computer Use returns `timeoutReached`; this is classified as `computer-use-timeout`.

No fresh real-local screenshot was committed for V2.72.

## Evidence Matrix

| Evidence | Accepted for | Not accepted for |
| --- | --- | --- |
| No-capture fixture smoke | build/service smoke health | real-local visual completion |
| Fixture capture smoke | fixture window screenshot when unlocked and nonblack/nonflat | replacing blocked real-local Computer Use |
| Screenshot artifact verifier | binary PNG health, privacy-string scan, black/flat/transparent rejection | OCR or manual visual layout judgment |
| Real-local Computer Use + app-window screenshot | final user-visible interaction and visual validation | locked, black, flat, desktop, or path-leaking evidence |

Canonical blocker labels: `locked-session`, `computer-use-timeout`, `window-not-found`, `remote-connection`, `no-ax-window`, `activation-failed`, `screen-recording-permission`, `black-capture`, `flat-capture`, `transparent-capture`, `invalid-capture`, `stale-bundle`, and `tool-layer-unknown`.

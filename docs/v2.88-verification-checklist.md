# V2.88 Verification Checklist

Status: completed on 2026-06-17.

V2.88 handoff and per-surface evidence closeout is completed. This slice
reviewed the staging scope, preserved the V2.87 implementation boundary, and
captured fresh app-window-only Computer Use evidence for the Agent Copilot
Lineup, Agent Profile, Local Session Preview, and MCP Preview surfaces.

## Scope

- Handoff and per-surface evidence review for the V2.87 Agent Copilot first
  implementation pass.
- Lineup surface evidence: decision queue, agent lineup, priorities, and
  evidence refs are visible.
- Agent Profile evidence: per-agent health/capability surface and default-off
  MCP Preview are visible.
- Local Session Preview evidence: default-off behavior and explicit authorized
  `/tmp/ac-v288` disposable fixture preview are visible.
- MCP Preview evidence: explicit authorized `/tmp/ac-v288` disposable config
  preview is visible with server metadata, args count, env-key count, and
  evidence refs.
- Version-doc gate wiring now includes V2.87 and V2.88 through
  `pnpm verify:v2.87-docs`, `pnpm verify:v2.88-docs`, and
  `pnpm verify:gate-parity`.

## Non-goals

- No product feature expansion beyond evidence closeout and documentation/gate
  synchronization.
- No adapter scan-root, writable-scope, install-scope, or config-schema change.
- No provider default calls.
- No write/apply path.
- No script execution.
- No credential reads.
- No raw prompt/response/trace persistence.
- No cloud sync.
- No telemetry.
- No public distribution, signing, notarization, DMG, or ZIP work.

## Coordinator Evidence Checklist

- [x] Current working tree was reviewed before staging.
- [x] Explorer audit found the current changed files broadly intentional for
  V2.87 implementation plus V2.88 closeout.
- [x] `pnpm check:macos` passed in an unlocked interactive macOS session.
- [x] Computer Use resolved the current workspace `dist/SkillsCopilot.app`.
- [x] Lineup app-window evidence was captured.
- [x] Agent Profile and MCP default-off app-window evidence was captured.
- [x] Local Session Preview default-off evidence was captured.
- [x] Local Session Preview authorized disposable fixture output was captured.
- [x] MCP Preview authorized disposable fixture output was captured.
- [x] Screenshot verifier passed for the V2.88 evidence directory.
- [x] `pnpm check:privacy` passed after evidence and docs updates.

## Verification Commands

```sh
pnpm check:macos                                           # passed
pnpm check:privacy                                         # passed
pnpm verify:screenshot-artifacts docs/ui-artifacts/v2.88-handoff-evidence # passed
pnpm verify:v2.88-docs                                     # passed
pnpm verify:gate-parity                                    # passed
git diff --check                                           # passed
```

## Real-local Evidence

- Bundle path: current workspace `dist/SkillsCopilot.app`.
- Main window identity: `skills-copilot.main-window`.
- Computer Use resolved the running app window titled `Agent Copilot`.
- Lineup screenshot: `docs/ui-artifacts/v2.88-handoff-evidence/lineup.png`.
- Agent Profile / MCP default-off screenshot:
  `docs/ui-artifacts/v2.88-handoff-evidence/agent-profile-mcp-default-off.png`.
- Local Session Preview default-off screenshot:
  `docs/ui-artifacts/v2.88-handoff-evidence/local-session-default-off.png`.
- Local Session Preview authorized output screenshot:
  `docs/ui-artifacts/v2.88-handoff-evidence/local-session-authorized-preview.png`.
- MCP Preview authorized output screenshot:
  `docs/ui-artifacts/v2.88-handoff-evidence/mcp-authorized-preview.png`.
- UI artifact notes:
  `docs/ui-artifacts/v2.88-handoff-evidence/verification.md`.

The authorized Local Session and MCP Preview checks used disposable `/tmp/ac-v288`
fixtures. They did not read default agent session stores, default MCP config
locations, user-home session directories, credentials, or real provider data.

## Safety Boundary

- Local Session Preview remained explicitly authorized and read-only; it returned
  redacted excerpts and `session.path` / `session.content_hash` refs only.
- MCP Preview remained explicitly authorized and read-only; it returned server
  metadata, args count, env-key count, and `mcp.config` / `mcp.server` refs only.
- The MCP fixture contained an env value, and the UI returned only `Env keys: 1`;
  it did not reveal the env value.
- No provider request, write/apply path, script execution, credential read, raw
  prompt/response/trace persistence, cloud sync, telemetry, or release packaging
  was added.
- Final status decision: completed.

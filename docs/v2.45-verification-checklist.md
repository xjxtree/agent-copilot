# V2.45 Verification Checklist（完成）

> Status: Completed on 2026-06-11. V2.45 routing confidence is delivered via local evidence-only `task.rankSkillRoutes` ranking flow after the V2.44 local-task readiness foundation.

## Scope

- User enters a task text and selected scope/candidate filters (when provided), then the system produces ranked candidate routing output instead of only flat readiness states.
- Ranking signals are based on local deterministic evidence: `metadata` / `findings` / `conflicts` / `analysis` / `adapter diagnostics` / `quality_score`.
- Output should remain **read-only by default** and preserve existing option-boundary provider paths: `llm.previewPrompt` + `llm.confirmPromptAndSend`.

## Completed checks

1. Focused Rust/Swift checks passed: `cargo test -p skills-copilot-service`、`cargo clippy -p skills-copilot-service --all-targets --all-features -- -D warnings`、`swift test --package-path apps/macos`。
2. `pnpm check:macos` passed.
3. Real local launch (`./script/build_and_run.sh run`) passed with Computer Use against `<repo>/dist/SkillsCopilot.app`; routing ranking returned candidate cards, confidence/band, match reasons, ambiguity/wrong-pick/miss risk, evidence references, and safety flags.
4. `pnpm check:privacy` passed.
5. App-window-only fixture screenshot was manually inspected; no desktop background, credentials, or real local path exposure was visible. No real local screenshot was saved because live UI exposes real local paths.
6. 文档齐套复核：
   - `AGENTS.md`
   - `docs/roadmap.md`
   - `docs/development-tasks.md`
   - `docs/ai-layer.md`
   - `docs/service-protocol.md`
   - `docs/security-model.md`

## Closeout 条件

- [x] 协议层定义并集成 `task.rankSkillRoutes`（返回 ranking + `confidence` + `match_reasons` + ambiguity/collision + wrong-pick/miss 风险）。
- [x] 路由输出在服务端契约中明确 `route_candidates`、`safety_flags` 和 `evidence_references`。
- [x] `task.checkReadiness` 保持可用且不变，V2.45 路由输出不改变其只读边界。
- [x] V2.45 remains local-only evidence ranking：不直接触发 config 写入、snapshot、triage mutation、script execution、provider send。
- [x] 真实本机验证后状态已更新为 completed。

## Closeout note

V2.45 real local validation also caught and fixed a Swift request-assembly bug where routing prompt preview sent `task_text` without `user_intent`; the fixed UI now sends both, and tests assert preview/confirm payloads carry the required task/user intent fields.

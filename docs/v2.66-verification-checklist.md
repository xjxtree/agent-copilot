# V2.66 Verification Checklist

Status: V2.66 Skill Lifecycle Timeline is complete after coordinator validation on 2026-06-12.

## Boundary

`skill.lifecycleTimeline` is user-triggered, deterministic/read-only, and uses local evidence only. It builds per-skill/per-agent/per-workspace lifecycle timeline rows from existing catalog evidence, scan/provenance/fingerprint state, stale/drift, finding/triage/remediation history, prompt run metadata, provider observability metadata, and session review outcomes where available.

It must not persist new raw lifecycle artifacts by default, write skill files or agent config, mutate triage, create or roll back snapshots, execute scripts, send provider/network requests by default, read credentials, persist raw prompt/response/trace/secrets/unredacted local paths, sync cloud data, or emit telemetry.

## Checklist

- [x] Focused Rust/protocol checks: `cargo fmt --all -- --check`, `cargo test -p skills-copilot-service --no-default-features lifecycle -- --nocapture`, and service protocol fixture decode passed.
- [x] Focused Swift/UI checks: `swift test --package-path apps/macos --filter SkillLifecycleTimeline`, focused SkillStore lifecycle coverage, and full `swift test --package-path apps/macos` passed.
- [x] Full checks: `pnpm check:macos` passed after the Clippy clamp cleanup, including Rust tests/clippy, native model/layout checks, Swift build/tests, fixture launch/window capture, and smoke coverage.
- [x] Real local Computer Use validation: current `dist/SkillsCopilot.app` launched and the project capture helper found an app window, but Computer Use returned `cgWindowNotFound` for the absolute app path and System Events reported 0 AX windows; a live screenshot was not committed because it can expose local paths.
- [x] Privacy check: `pnpm check:privacy` passed and the fixture screenshot was inspected as fixture-only.
- [x] Docs/schema consistency: README, AGENTS, AI layer, architecture, data model, roadmap, security model, service protocol, protocol fixtures/schema notes, and UI text agree on `skill.lifecycleTimeline`, completed status, local-only evidence sources, safety flags, and V2.67 planned status.
- [x] Git diff check: `git diff --check` and `git diff --cached --check` passed.

# V2.43 Verification Checklist

> Status: Completed. V2.43 `AI Skill Quality Score` adds a user-triggered, read-only deterministic local score plus an optional provider explanation path that must flow through V2.42 prompt preview/redaction/confirmation.

1. Focused Rust/Swift checks：`cargo test --workspace`、`cargo clippy --workspace --all-targets --all-features`、`swift test --package-path apps/macos`。
2. `pnpm check:macos`。
3. Real local launch (`./script/build_and_run.sh run` 或 `pnpm dev:macos`) 并进行 `SkillsCopilot` 窗口 `Computer Use`/AX 观察；若窗口不可见，记录 blocker。
4. `pnpm check:privacy`。
5. App-window-only 截图后手工复核路径/凭据占位符脱敏。
6. 复核 V2.43 口径：
   - 质量评分为 user-triggered、read-only 调用；默认不产生后台/周期性评分。
   - 输入证据仅为 deterministic local evidence（metadata、findings、conflicts、analysis、adapter diagnostics）。
   - optional 的 provider 路径必须显示 prompt 预览、included/excluded 字段、redaction summary、token/cost 预估、destination，并在用户确认后才发起调用。
   - provider-backed 结果为 copy-only draft/summary，不具备直接 Apply/Write 路径。
7. 复核关闭边界：无后台 provider 轮询，无 script execute，无 telemetry，无 cloud sync，未引入 AI 自动 write-back / config mutation / snapshot 变更 / triage 变更。
8. 复核隐私边界：不持久化 raw prompt/response，不在 report/export 默认落地 raw secrets，不新增凭据泄露路径；API key 仍走 provider profile 的 Keychain-first + 最小 metadata 规则（V2.41/42 继续生效）。

**Closeout 条件**

- `analysis.scoreSkillQuality` 在 service protocol 中可用，并具备 V2.42 prompt preview + explicit confirmation 的可选 provider explanation path。
- 每次 provider-backed 可选评分在发送前都经过 prompt preview 并包含 redaction summary / included-excluded / token-cost / destination。
- 评分结果默认只读，不触发 `config.toggleSkill` / `snapshot.*` / triage / 脚本执行 / 凭据持久化。
- 文档同步：`roadmap.md` / `development-tasks.md` / `ai-layer.md` / `service-protocol.md` / `AGENTS.md` 均记录 V2.43 的实现状态与验证结果。
- 无新 blocker；real local Computer Use passed by explicitly targeting the current `dist/SkillsCopilot.app` bundle. No real local screenshot was saved because the live UI exposes local paths.

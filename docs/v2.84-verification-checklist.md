# V2.84 Verification Checklist

Status: completed on 2026-06-16.

V2.84 Swift Detail section splitting is completed. This is a refactor-only native UI module-boundary slice that keeps the existing Detail IA, labels, accessibility identifiers, and Task Cockpit / Skill Map / Guided Cleanup / Provider Observability / Review semantics unchanged.

## Scope

- `DetailView.swift` is now a small routing and composition file.
- Detail section views moved into dedicated Swift files: `DetailReviewCoreSection.swift`, `DetailReviewKnowledgePanels.swift`, `DetailRemediationPanels.swift`, `DetailKnowledgeSkillMapPanels.swift`, `DetailGuidedCleanupFlowPanel.swift`, `DetailProviderObservabilityPanel.swift`, `DetailLocalSkillMapViews.swift`, `DetailTaskBenchmarkSection.swift`, `DetailAgentSessionSection.swift`, `DetailLLMSection.swift`, `DetailHeaderOverviewSection.swift`, and `DetailFindingsHistorySection.swift`.
- Existing dedicated panels remain dedicated: `TaskCockpitPanel.swift` and `ValidationWorkbenchPanel.swift`.
- `scripts/verify-native-ui-layout.mjs` aggregates the split Detail files so previous UI layout, privacy, evidence-list, Task Cockpit, Guided Cleanup, Provider Observability, Review, and validation workbench guards remain active.
- No fresh Computer Use screenshot is required because V2.84 changes source-file boundaries only and does not change user-visible native UI behavior.

## Non-goals

- No new UI surface, label, navigation behavior, or product workflow.
- No service protocol method or payload changes.
- No protocol version bump.
- No provider default calls.
- No write/apply path.
- No hidden task state.
- No scanner/catalog fact mutation.
- No script execution.
- No credential reads.
- No raw prompt/response/trace persistence.
- No cloud sync.
- No telemetry.
- No public distribution, signing, notarization, DMG, or ZIP work.

## Coordinator Evidence Checklist

- [x] `DetailView.swift` is reduced below 5k lines and acts as the Detail router/composer.
- [x] Task Cockpit and Validation Workbench remain in existing dedicated files.
- [x] Skill Map, Guided Cleanup, Provider Observability, Review, findings/history, LLM, and remediation views are split into dedicated files.
- [x] Native UI verifier aggregates the split Detail surface.
- [x] Swift tests are run and recorded after the split.
- [x] Native UI layout verifier is run and recorded after the split.
- [x] Module-size verifier is run and recorded after the split.
- [x] Gate parity includes the V2.84 docs verifier.
- [x] Real-local validation decision is recorded: no fresh UI evidence required for this refactor-only source split.

## Closeout Verification Commands

```sh
swift test --package-path apps/macos      # passed
pnpm verify:macos-ui-layout               # passed
pnpm verify:module-size                   # passed
pnpm verify:v2.84-docs                    # passed
pnpm verify:gate-parity                   # passed
pnpm check:privacy                        # passed
git diff --check                          # passed
```

## Evidence Status

- Coordinator run date: 2026-06-16.
- Implementation evidence: completed in `apps/macos/Sources/SkillsCopilot/Views/DetailView.swift`, the new split `Detail*.swift` files, `scripts/verify-native-ui-layout.mjs`, `scripts/verify-module-size.mjs`, and `package.json`.
- Focused Swift evidence: `swift test --package-path apps/macos` passed after the Detail section split.
- Static UI evidence: `pnpm verify:macos-ui-layout` passed with the split Detail surface aggregated.
- File-size evidence: `pnpm verify:module-size` passed and verifies the split Detail files are <= 5000 lines.
- Real-local UI evidence or canonical blocker: fresh UI evidence is not required because V2.84 has no user-visible native UI behavior change.
- Final status decision: completed.

## Safety Boundary

V2.84 remains a refactor-only Swift source split:

- No new UI surface, label, navigation behavior, or product workflow.
- No service protocol method or payload changes.
- No protocol version bump.
- No provider default calls.
- No write/apply path.
- No hidden task state.
- No scanner/catalog fact mutation.
- No script execution.
- No credential reads.
- No raw prompt/response/trace persistence.
- No cloud sync.
- No telemetry.
- No public distribution, signing, notarization, DMG, or ZIP work.

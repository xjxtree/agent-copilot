# V2.89 Verification Checklist

Status: completed on 2026-06-17.

V2.89 Brand asset refresh is completed. This slice refreshes the Agent Copilot
display brand icon assets while keeping internal identifiers unchanged for the
future V2.90 migration slice.

Closeout keyword: unchanged internal identifiers.

## Scope

- Added a reviewable vector source at
  `apps/macos/Sources/SkillsCopilot/Resources/AppIcon.svg`.
- Regenerated `apps/macos/Sources/SkillsCopilot/Resources/AppIcon.icns`.
- Added `script/generate_app_icon.sh` and `pnpm generate:app-icon` so the icon
  can be reproduced from source.
- Verified `dist/SkillsCopilot.app` still bundles `AppIcon.icns` through the
  existing build/run path.
- Captured V2.89 app-window-only evidence under
  `docs/ui-artifacts/v2.89-brand-assets/`.

## Non-goals

- No internal bundle/module/AX/app-data identifier migration.
- No `APP_NAME`, `BUNDLE_ID`, Swift package/product/target/module, or AX
  identifier rename.
- No service protocol method, payload, or version change.
- No adapter scan-root, writable-scope, install-scope, or config-schema change.
- No provider default calls.
- No write/apply path.
- No script execution path exposed to the product.
- No credential reads.
- No raw prompt/response/trace persistence.
- No cloud sync.
- No telemetry.
- No public distribution, signing, notarization, DMG, or ZIP work.

## Coordinator Evidence Checklist

- [x] Existing icon asset pipeline was reviewed.
- [x] `AppIcon.svg` source was added for the Agent Copilot display brand.
- [x] `AppIcon.icns` was regenerated from `AppIcon.svg`.
- [x] `pnpm generate:app-icon` is available for reproducible local regeneration.
- [x] `script/build_and_run.sh` continues to use `CFBundleIconFile=AppIcon`.
- [x] `APP_NAME="SkillsCopilot"` and `BUNDLE_ID="dev.skills-copilot.native"`
  remain unchanged.
- [x] Fresh app-window evidence was captured.
- [x] Screenshot verifier passed for the V2.89 evidence directory.
- [x] Full macOS gate passed after the asset refresh.
- [x] Privacy gate passed after asset/docs updates.

## Verification Commands

```sh
pnpm generate:app-icon                                  # passed
pnpm check:macos                                        # passed
pnpm verify:screenshot-artifacts docs/ui-artifacts/v2.89-brand-assets # passed
pnpm verify:v2.89-docs                                  # passed
pnpm verify:gate-parity                                 # passed
pnpm check:privacy                                      # passed
git diff --check                                        # passed
```

## Evidence

- Icon source: `apps/macos/Sources/SkillsCopilot/Resources/AppIcon.svg`.
- Bundled icon source: `apps/macos/Sources/SkillsCopilot/Resources/AppIcon.icns`.
- UI evidence directory: `docs/ui-artifacts/v2.89-brand-assets/`.
- App-window evidence: `docs/ui-artifacts/v2.89-brand-assets/completed.png`.
- Verification notes: `docs/ui-artifacts/v2.89-brand-assets/verification.md`.
- Bundle path stayed `dist/SkillsCopilot.app`.
- Internal identifiers stayed `SkillsCopilot` / `skills-copilot`.
- Final status decision: completed.

## Safety Boundary

The refreshed brand asset is visual-only and build-time only. It does not add
provider requests, network calls, product script execution, skill-file writes,
agent-config writes, snapshot/rollback mutation, credential access, raw
prompt/response/trace persistence, cloud sync, telemetry, or release packaging.

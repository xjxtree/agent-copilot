# V2.80 Verification Checklist

Status: completed on 2026-06-15.

V2.80 Detail navigation and visual density polish is completed. This slice resets the Detail scroll position when users switch major Work/detail surfaces and tightens dense evidence presentation with counted, collapsible, screenshot-safe lists for real catalog data.

## Boundary

- Detail navigation: `DetailView` owns a stable `skills-copilot.detail.top` anchor and scrolls to it when `selectedDetailSection` changes.
- Visual density: `DenseCountBadge` and `DenseDisclosureList` cap long evidence/safety lists while preserving overflow behind an explicit disclosure.
- Safety metadata: compact evidence rows still render blocker, finding, source, and safety-boundary metadata through screenshot-safe privacy labels.
- User-visible validation: V2.80 required fresh unlocked real-local Computer Use and app-window screenshot evidence; fixture smoke remains supporting evidence only.
- Gate parity: `pnpm verify:v2.80-docs` is part of the shared `pnpm verify:gate-parity` lane.

## Non-goals

- No service method additions, renames, or payload expansion.
- No provider default calls or background network calls.
- No write/apply path, skill file mutation, agent config mutation, triage mutation, hidden task state, snapshot creation, or rollback behavior change.
- No scanner/catalog fact mutation.
- No script execution or script preview behavior change.
- No credential reads.
- No raw prompt, raw response, raw trace, secret, or unredacted path persistence.
- No cloud sync, telemetry, public distribution, signing, notarization, DMG, or ZIP automation.
- No weakening of V2.72-V2.79 validation evidence standards.

## Coordinator Evidence Checklist

- [x] Code implements Detail navigation and visual density polish.
- [x] Focused Swift/UI verifier coverage proves scroll reset behavior for Work/detail navigation.
- [x] Focused Swift/UI verifier coverage proves dense evidence cards, long lists, summary rows, and two-column layouts remain readable.
- [x] Docs describe V2.80 scope, evidence requirements, and safety boundary.
- [x] `pnpm verify:v2.80-docs` was run and passed.
- [x] `pnpm verify:v2.79-docs` was run and passed after script wiring.
- [x] `pnpm verify:gate-parity` was run and passed after script wiring.
- [x] Focused implementation-owner checks for touched Swift UI/tests were run and passed.
- [x] `pnpm check:macos` was run and passed after implementation landed.
- [x] `pnpm check:privacy` was run and passed after screenshot/docs evidence changes.
- [x] `git diff --check` was run and passed.
- [x] Fresh unlocked real-local Computer Use targeted the current workspace `dist/SkillsCopilot.app` by exact bundle path/PID and recorded the main window identity.
- [x] Computer Use exercised affected Detail navigation / Work surface switching and verified coherent scroll position after switching from a scrolled Review surface to Validation Workbench.
- [x] Computer Use verified dense real-catalog evidence remained readable and preserved safety metadata.
- [x] App-window screenshot evidence was captured under `docs/ui-artifacts/v2.80-detail-density/`.
- [x] Manual visual review confirmed the screenshot is app-window-only, nonblack, nonflat, nontransparent, readable, and free of visible local paths or secrets.
- [x] Safety boundaries were confirmed: no service method, provider default call, write/apply path, hidden task state, scanner/catalog fact mutation, script execution, credential read, raw prompt/response/trace persistence, cloud sync, telemetry, or public distribution expansion.

## Verification Commands

```sh
swift test --package-path apps/macos                    # passed
pnpm verify:macos-ui-layout                             # passed
pnpm verify:v2.80-docs                                  # passed
pnpm verify:v2.79-docs                                  # passed
pnpm verify:gate-parity                                 # passed
pnpm check:macos                                        # passed
pnpm check:privacy                                      # passed
pnpm verify:screenshot-artifacts                        # passed
git diff --check                                        # passed
```

Focused implementation-owner checks also passed before coordinator closeout:

- `pnpm verify:macos-ui-layout` covers the `ScrollViewReader` top anchor, `selectedDetailSection` scroll reset, `DenseDisclosureList`, counted/collapsible evidence lists, and screenshot-safe evidence rendering.
- `swift test --package-path apps/macos` covers the native Swift test package after the Detail UI changes.
- `git diff --check` passed after docs, verifier, Swift UI, and screenshot evidence changes.

## Real-local Evidence

V2.80 is user-visible because it changes Detail navigation and visual density. Closeout used fresh unlocked real-local Computer Use evidence against the current workspace bundle:

- Bundle path: current workspace `dist/SkillsCopilot.app`.
- Running PID: `82571`.
- Main window identity: `skills-copilot.main-window`.
- Affected surfaces exercised: selected a real catalog skill, opened `复查`, scrolled the long Review detail surface to the mid-panel quality area, then selected `验证工作台`.
- Scroll-position result: after switching sections, the right Detail scroll area returned to the top with `详情区域`, picker value `验证工作台`, `skills-copilot.validation-workbench`, summary, evidence standards, and canonical blocker rows visible.
- Dense evidence result: real catalog evidence cards remained readable, showed compact/counted rows, used screenshot-safe `$HOME` path presentation with reveal controls where needed, and preserved read-only / blocked safety metadata.
- Screenshot evidence: [`docs/ui-artifacts/v2.80-detail-density/completed.png`](../ui-artifacts/v2.80-detail-density/completed.png).
- Capture result: exact PID capture succeeded with window `37734`, PID `82571`, app-window size `920x652`, and PNG output `1840x1304`.
- Manual visual review confirmed the screenshot is app-window-only, nonblack, nonflat, nontransparent, readable, localized in Chinese for primary workflows, and does not expose raw local paths.

Fixture smoke screenshots and smoke success are supporting evidence only. They did not replace the fresh unlocked real-local Computer Use evidence for V2.80 closeout.

## Closeout Notes

- Coordinator run date: 2026-06-15.
- Coordinator command log: passed commands listed above.
- Final status decision: completed.

## Safety Boundary

V2.80 remains a native UI polish and validation-evidence slice:

- No service method additions, renames, or payload expansion.
- No provider default calls.
- No write/apply path.
- No hidden task state.
- No scanner/catalog fact mutation.
- No skill file or agent config mutation.
- No triage mutation.
- No snapshot or rollback behavior change.
- No script execution.
- No credential reads.
- No raw prompt/response/trace persistence.
- No cloud sync.
- No telemetry.
- No public distribution automation.
- No weakening of V2.72-V2.79 validation evidence standards.

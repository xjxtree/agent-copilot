# V2.74 Verification Checklist

Status: completed on 2026-06-15. V2.74 closes Real-local launch and window targeting stability with exact current-bundle launch, duplicate same-bundle fail-closed handling, CG/AX/Computer Use identity evidence, and app-window-only screenshot evidence.

V2.74 scope: make real-local app launch and validation target the current workspace bundle deterministically. The closeout must remove stale same-bundle ambiguity, make current bundle path/PID/window identity diagnosable, and require unlocked Computer Use evidence. This slice must not expand into signing, notarization, DMG/ZIP packaging, public distribution, provider calls, write paths, script execution, cloud sync, telemetry, or credential handling changes.

## Required Closeout Evidence

- [x] Rebuild and launch the current workspace `dist/SkillsCopilot.app`, then record the exact absolute bundle path, bundle id `dev.skills-copilot.native`, executable path, and running PID used for validation.
- [x] Detect duplicate running `SkillsCopilot` processes with the same bundle id before Computer Use validation; either terminate stale same-bundle instances from other worktrees or record a canonical blocker.
- [x] Prove the observed CG window belongs to the current workspace PID, not an older app bundle or LaunchServices-resolved duplicate.
- [x] Prove AX window discovery sees the same current app process and reports at least one app window before interaction evidence is accepted.
- [x] Target Computer Use by the exact workspace app path first; app-name targeting may only be supporting evidence after path/PID identity is established.
- [x] Confirm the macOS session is unlocked and interactive before collecting real-local Computer Use or screenshot evidence.
- [x] Exercise at least one visible real-local app workflow with Computer Use read-back against the current `dist/SkillsCopilot.app`.
- [x] Capture only the app window after real-local validation, verify the screenshot is nonblack, nonflat, nontransparent, and free of visible local paths, secrets, raw prompts, raw responses, raw traces, and credentials.
- [x] Run the shared validation and privacy gates listed below.

## Completed Evidence

- Build and launch: `./script/build_and_run.sh --verify` rebuilt and launched the current workspace bundle at `<repo>/dist/SkillsCopilot.app` with bundle id `dev.skills-copilot.native`, executable `<repo>/dist/SkillsCopilot.app/Contents/MacOS/SkillsCopilot`, and running PID `52193`.
- Duplicate handling: an intentionally parallel launch/smoke overlap failed closed with `activation-failed` for duplicate current-bundle PIDs `49968 50483`; the sequential validation rerun then succeeded with one retained current-bundle process. This proves duplicate same-bundle ambiguity is treated as a blocker instead of accepted evidence.
- AX and Computer Use identity: Computer Use targeted the exact workspace app path, resolved bundle id `dev.skills-copilot.native`, PID `52193`, window title `SkillsCopilot`, main window ID `skills-copilot.main-window`, Task Cockpit panel ID `skills-copilot.task-cockpit.panel`, input ID `skills-copilot.task-cockpit.input`, build button ID `skills-copilot.task-cockpit.build`, status ID `skills-copilot.task-cockpit.status`, and result ID `skills-copilot.task-cockpit.result`.
- Interaction evidence: Computer Use set task text `V2.74 current bundle window targeting validation`, clicked `Generate Cockpit`, and read back the fallback/partial result state with retry controls and local result rows. No provider request, write action, script execution, credential read, cloud sync, or telemetry path was introduced.
- CG capture evidence: `script/capture_app_window.sh <repo>/dist/SkillsCopilot.app docs/ui-artifacts/v2.74-launch-window-targeting/completed.png` captured window id `36200` owned by PID `52193`.
- Screenshot evidence: [`docs/ui-artifacts/v2.74-launch-window-targeting/completed.png`](./ui-artifacts/v2.74-launch-window-targeting/completed.png) was manually inspected and passed screenshot artifact verification as an app-window-only, nonblack, nonflat, nontransparent capture without visible local paths, secrets, raw prompts, raw responses, raw traces, or credentials.

## Exact Workspace Bundle and PID Targeting

Closeout evidence must identify the app being validated without relying on ambiguous app-name lookup alone:

- Current workspace bundle path: `<repo>/dist/SkillsCopilot.app`.
- Bundle id: `dev.skills-copilot.native`.
- Running process PID and executable path for the launched app.
- CG window id and owning PID for the visible `SkillsCopilot` window.
- AX process/window lookup result for the same PID.
- Computer Use target value, with the absolute app path preferred for the primary pass.

If any of these identities disagree, V2.74 cannot be marked complete. Record the mismatch as `stale-bundle`, `window-not-found`, `no-ax-window`, or `tool-layer-unknown`, depending on the failure.

## Duplicate Same-bundle Detection

V2.74 must explicitly handle the recurring same-bundle validation failure mode:

- Detect more than one running `SkillsCopilot` process with bundle id `dev.skills-copilot.native`.
- Distinguish the current workspace `dist/SkillsCopilot.app` from older worktree bundles.
- Avoid accepting a window from a stale bundle as current evidence.
- Prefer deterministic cleanup/relaunch before validation; if cleanup is unsafe or inconclusive, stop and record a canonical blocker.
- Document the duplicate-process state and the exact PID retained for validation.

## Canonical Blocker Handling

Fixture smoke, fixture screenshots, no-capture launch success, and app-name-only lookup are supporting evidence only. They cannot replace blocked real-local Computer Use evidence.

If validation cannot complete, record exactly one primary canonical blocker and do not mark V2.74 completed:

- `locked-session`
- `window-not-found`
- `no-ax-window`
- `computer-use-timeout`
- `remote-connection`
- `activation-failed`
- `black-capture`
- `flat-capture`
- `transparent-capture`
- `invalid-capture`
- `screen-recording-permission`
- `stale-bundle`
- `tool-layer-unknown`

The blocker record must include the failed command or tool step, the exact target path/PID when available, the classifier output, and the next retry condition.

## Unlocked Real-local Computer Use Evidence

Completion requires an unlocked, interactive macOS session and real-local Computer Use against the current workspace bundle:

- Target: current workspace `dist/SkillsCopilot.app`.
- Required identity evidence: exact app path, PID, bundle id, CG window id, and AX window match.
- Required interaction evidence: Computer Use state read-back after at least one visible app workflow.
- Required screenshot evidence: app-window-only capture after the interaction, verified by screenshot artifact checks and manual privacy inspection.

If Computer Use cannot resolve or operate the current app window, record a blocker. Do not substitute fixture screenshots, direct CG captures, or smoke success for the missing Computer Use interaction.

## No Signing, Notarization, or Distribution Scope

V2.74 is local validation stability work only:

- No formal signing or certificate workflow.
- No notarization.
- No DMG or ZIP packaging.
- No public distribution or release channel work.
- No full-platform UI adaptation.
- No provider default calls.
- No skill/config writes.
- No triage mutation.
- No snapshot creation or rollback.
- No script execution.
- No credential reads.
- No raw prompt, raw response, raw trace, secret, or unredacted local path persistence.
- No cloud sync.
- No telemetry.

## Expected Commands

```sh
pnpm verify:v2.74-docs
pnpm verify:validation-blockers
./script/build_and_run.sh --verify
pnpm smoke:macos-app -- --fixture-data --capture-window
pnpm verify:screenshot-artifacts
pnpm check:privacy
pnpm check:macos
git diff --check
```

## Current Integration Notes

- V2.74 implementation, tests, docs closeout, and unlocked real-local Computer Use evidence are complete.
- Future launch/window validation changes must preserve exact workspace bundle/PID targeting, duplicate same-bundle blocker behavior, and the V2.72 canonical evidence standard.

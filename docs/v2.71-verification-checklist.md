# V2.71 Verification Checklist

V2.71 completed Guided Cleanup safe-action deep links.

## Scope

- Added `safe_action_deep_link` to guided cleanup flow steps.
- Added `deep_link` to guided cleanup safe next actions.
- Rendered safe-link buttons in the native Guided Cleanup UI.
- Routed safe links only to existing read-only or preview-first surfaces: cleanup/detail sections, remediation plan/drafts/impact/batch review, skill lifecycle, task cockpit, safe batch preview panel context, and guided cleanup metadata recording.
- Mounted remediation plan, fix preview drafts, impact preview, batch review, and remediation history panels inside Analysis so linked safe entries are visible.
- Preserved no-apply semantics: no direct config toggle, batch apply, script execution, provider confirmation, triage mutation, snapshot creation/rollback, credential read, cloud sync, or telemetry.

## Commands

1. `cargo fmt --all -- --check`
2. `cargo test -p skills-copilot-service --lib guided_cleanup`
3. `cargo test -p skills-copilot-service --lib service_protocol_fixtures_decode`
4. `cargo test -p skills-copilot-service --lib supported_methods_have_dispatch_coverage`
5. `swift test --package-path apps/macos`
6. `pnpm verify:macos-ui-layout`
7. `pnpm verify:screenshot-artifacts`
8. `./script/build_and_run.sh --verify`
9. `pnpm smoke:macos-app -- --fixture-data`
10. `pnpm check:privacy`
11. `git diff --check`

## Full Gate Result

`pnpm check:macos` passed build/test/service stages and failed closed at fixture capture with:

```text
locked-session: macOS session is locked; refusing to create screenshot evidence
```

## Real Local Note

Computer Use returned `timeoutReached`. No fresh V2.71 screenshot was committed. Real-local visual validation, including screenshot-based visual inspection, must be rerun in an unlocked interactive macOS session.

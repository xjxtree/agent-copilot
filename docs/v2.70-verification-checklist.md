# V2.70 Verification Checklist

V2.70 completed Swift / Rust feature modularization.

## Scope

- Extracted Swift Task Cockpit rendering into `TaskCockpitPanel.swift`.
- Extracted shared Swift detail presentation primitives into `DetailPresentationPrimitives.swift`.
- Extracted Rust cleanup queue DTOs and `ServiceHost::cleanup_list_queue` into `cleanup_queue.rs`.
- Updated native UI layout verification to aggregate split detail files.
- No JSON-RPC method, payload, persistence, scanner/catalog, provider, write, triage, snapshot, script, cloud, or telemetry semantics were changed.

## Commands

1. `cargo fmt --all -- --check`
2. `cargo test -p skills-copilot-service --lib cleanup_queue`
3. `cargo test -p skills-copilot-service --lib supported_methods_have_dispatch_coverage`
4. `cargo test -p skills-copilot-service --lib service_protocol_fixtures_decode`
5. `swift build --package-path apps/macos`
6. `swift test --package-path apps/macos`
7. `pnpm verify:macos-ui-layout`
8. `pnpm verify:screenshot-artifacts`
9. `pnpm smoke:macos-app -- --fixture-data`
10. `pnpm check:privacy`
11. `git diff --check`
12. `git diff --cached --check`

## Full Gate Result

`pnpm check:macos` passed build/test/service stages and failed closed at fixture capture with `locked-session: macOS session is locked; refusing to create screenshot evidence`.

## Real Local Note

Computer Use returned `timeoutReached`; `ioreg` reported `CGSSessionScreenIsLocked=Yes`; direct capture exited 6 with `locked-session`. Full app-window screenshot capture still requires an unlocked interactive macOS session. Locked-session, black-capture, flat-capture, Screen Recording permission, window-not-found, `cgWindowNotFound`, `remoteConnection`, or other Computer Use tool-layer failures are blockers and cannot be counted as completed visual validation.

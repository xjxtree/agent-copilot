# V2.44 Verification Checklist（完成）

> Status: Completed after integration. V2.44 task readiness is implemented in the Rust service protocol and native macOS Analysis UI, while preserving the V2.42 prompt preview/redaction/confirmation boundary for optional provider explanations.

## Scope

- User enters a real task (plain text), and the system evaluates local candidate coverage in read-only form.
- Candidate classes: `available / enabled / scoped / risky / missing`.
- Signal inputs are local deterministic evidence: `metadata` / `findings` / `conflicts` / `analysis` / `adapter diagnostics` and optional `analysis.scoreSkillQuality`.
- Results are read-only previews by default.

## Completed closeout checks

1. Focused Rust/Swift checks: `cargo test -p skills-copilot-service` and `swift test --package-path apps/macos`.
2. Full macOS gate: `pnpm check:macos`.
3. Real local app validation: launch `dist/SkillsCopilot.app` and operate the V2.44 Analysis readiness flow with Computer Use against the current bundle path.
4. 运行 `pnpm check:privacy`。
5. 文档同步复核：
   - `AGENTS.md`
   - `docs/roadmap.md`
   - `docs/development-tasks.md`
   - `docs/ai-layer.md`
   - `docs/service-protocol.md`
   - `docs/security-model.md`

   均将 V2.44 标注为**完成 / 已集成**。
6. 服务协议与安全边界口径复核：
   - `task.checkReadiness` 是可用的 read-only service method。
   - 结果仅为 read-only 评估，不触发 `config.toggleSkill` / `snapshot.*` / triage mutation / credentials mutation。
   - Optional provider-backed 解释仍需要 `llm.previewPrompt` + `llm.confirmPromptAndSend`。
7. 用户隐私与安全边界复核：
   - 无 background/周期性 readiness 重算。
   - 无 AI write-back / script execution。
   - 无 raw prompt / raw response 默认持久化。
   - Credentials 仍保持 Keychain-first / `~/.config/skills-copilot/llm.yaml` fallback `0600`。

## Closeout 条件

- [x] 在 service protocol 中新增可用的 V2.44 readiness method：`task.checkReadiness`，支持 task text、agent filter、candidate ids 与 result limit。
- [x] read-only 输出说明 score/band、candidate skills、enabled/scope/risk state、missing gaps、blockers、evidence references 与 safety flags。
- [x] Optional provider 解释路径保留 V2.42 的 prompt 预览 / redaction / confirmation 流程，并不提供 apply/write/execute/triage mutation。
- [x] 文档与任务台账标记 V2.44 complete，后续 routing confidence 留给 V2.45。

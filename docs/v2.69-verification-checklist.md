# V2.69 Verification Checklist

V2.69 completed Privacy / Screenshot Mode + localization polish.

## Scope

- App-local screenshot privacy mode defaults on.
- Native UI path presentation redacts local `HOME`, project root/cwd, app-data, and temp roots, collapses long paths, and reveals full paths only by explicit local UI action.
- Task Cockpit, Guided Cleanup, and Provider Observability primary labels are localized instead of falling back to English.
- `task.buildCockpit` and `cleanup.planGuidedFlow` include `app_language` for localized service rows.
- Screenshot capture rejects locked-session, near-black, mostly transparent, and near-flat captures.
- No scanner/catalog fact mutation, skill/config writes, snapshots, triage mutation, script execution, provider default request, cloud sync, or telemetry was added.

## Commands

1. `plutil -lint apps/macos/Sources/SkillsCopilot/Resources/en.lproj/Localizable.strings apps/macos/Sources/SkillsCopilot/Resources/zh-Hans.lproj/Localizable.strings`
2. `pnpm test:macos-list-model`
3. `pnpm verify:macos-ui-layout`
4. `pnpm verify:screenshot-artifacts`
5. `swift test --package-path apps/macos`
6. `pnpm smoke:macos-app -- --fixture-data`
7. `pnpm check:macos` reached fixture capture and failed closed with `locked-session: macOS session is locked; refusing to create screenshot evidence`
8. `pnpm check:privacy`
9. `git diff --check`
10. `git diff --cached --check`

## Screenshot Evidence

- No fresh V2.69 fixture screenshot was committed from the locked session.
- `pnpm check:macos` must be rerun in an unlocked interactive session before adding `docs/ui-artifacts/v2.69-privacy-screenshot-mode/completed.png`.
- `pnpm verify:screenshot-artifacts` validates committed PNG artifacts are readable and not black/flat captures. It does not perform OCR; visual inspection remains required for any new screenshot.

## Real Local Note

Current locked-session attempt: Computer Use returned `timeoutReached`, `ioreg` reported `CGSSessionScreenIsLocked=Yes`, and direct capture exited with `locked-session`.

Real-local Computer Use must be retried when the macOS session is unlocked and interactive. Locked-session, black-capture, flat-capture, Screen Recording permission, window-not-found, `cgWindowNotFound`, `remoteConnection`, or other Computer Use tool-layer failures are blockers and cannot be counted as completed visual validation.

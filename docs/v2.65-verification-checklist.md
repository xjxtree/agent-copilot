# V2.65 Verification Checklist（completed）

> Status: V2.65 Task-first Cockpit is complete as of 2026-06-12. Coordinator validation passed the focused Rust/Swift/protocol checks, full service/Swift checks, `pnpm check:macos`, `pnpm check:privacy`, and fixture screenshot inspection. Real local validation launched the current bundle process, but the macOS window automation layer reported the V2.65 window/tool-layer blocker described below.

## Scope

- Task-first Cockpit is local evidence-first and read-only by default.
- It aggregates task readiness, routing confidence, benchmark/regression, trace/session review, provider-run metadata, remediation next steps, gaps/blockers, evidence refs, prompt metadata, and safety flags.
- It must not create a new hidden task-state source, send provider traffic by default, write skill files/config, mutate triage/snapshots, execute scripts, read credentials, persist raw prompt/response/trace, sync cloud data, or emit telemetry.

## Validation checklist

- [x] Focused Rust/protocol checks: `cargo test -p skills-copilot-service --no-default-features task_cockpit -- --nocapture` and `cargo test -p skills-copilot-service --no-default-features service_protocol_fixtures_decode -- --nocapture`.
- [x] Focused Swift/UI checks: `swift test --package-path apps/macos --filter TaskCockpitModelTests` and `swift test --package-path apps/macos --filter SkillStoreTests`.
- [x] Full service and Swift checks: `cargo test -p skills-copilot-service --no-default-features` and `swift test --package-path apps/macos`.
- [x] `pnpm check:macos`
- [x] Real local launch attempt: `./script/build_and_run.sh` launched the current `dist/SkillsCopilot.app` process; System Events confirmed the `SkillsCopilot` process, but reported `0` windows after activation, and Computer Use returned `cgWindowNotFound` for the absolute bundle path. This is recorded as the V2.65 window/tool-layer blocker. No real-local screenshot was committed because live UI can expose local paths.
- [x] `pnpm check:privacy`
- [x] Docs/schema consistency: `AGENTS.md`, `README.md`, `docs/development-tasks.md`, `docs/roadmap.md`, `docs/service-protocol.md`, `docs/data-model.md`, `docs/ai-layer.md`, `docs/security-model.md`, and `docs/architecture.md` agree on V2.65 complete and V2.66-V2.67 planned.
- [x] `git diff --check`

## Closeout criteria

- [x] Service protocol and data model remain aligned on the Task-first Cockpit response shape.
- [x] Security and AI-layer boundaries still forbid default provider traffic, hidden task state, write paths, snapshots, triage mutation, script execution, credential reads, raw prompt/response persistence, cloud sync, and telemetry.
- [x] Coordinator validation recorded; V2.65 is final complete with the explicit window/tool-layer blocker above.

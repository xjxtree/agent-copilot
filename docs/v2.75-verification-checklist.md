# V2.75 Verification Checklist

Status: completed on 2026-06-15. V2.75 Task input and input-method resilience is complete.

V2.75 scope: Task input and input-method resilience for the Task Cockpit task entry path. The closeout proves that committed Chinese text, pasted text, multiline tasks, leading/trailing whitespace, emoji, automation input, explicit submit, focus restoration, and real-local Computer Use evidence all behave predictably before task readiness, routing, remediation, or cockpit service calls run.

V2.75 is an input resilience and validation slice over existing read-only Task Cockpit flows. It does not add raw prompt persistence, cloud sync, provider default calls, write paths, script execution, credential reads, telemetry, broad text editor rewrite, scanner/catalog fact changes, new analytic endpoints, or hidden task state.

## Completed Boundary

- Chinese input method composition: committed Chinese text is preserved for the explicit task submit path; ordinary text editing and focus movement do not submit.
- Paste handling: the input accepts mixed Chinese/English text, punctuation, line breaks, emoji, and internal spacing without input-method corruption.
- Multiline task entry: users can enter and review multiline tasks before explicit submit; line breaks do not trigger execution by themselves.
- Leading/trailing whitespace: blank detection trims whitespace, while nonblank service-call text preserves the exact raw user input.
- Emoji and uncommon characters: task text containing emoji or non-ASCII characters remains readable in the field and submitted task state.
- Automation input: Computer Use can set the task field through the stable `skills-copilot.task-cockpit.input` accessibility identifier without active-input-method corruption.
- Explicit submit: task execution occurs through the visible Build / Generate Cockpit action, not during composition, focus movement, or ordinary newline editing.
- Focus restoration: after submit and fallback result rendering, the editable task text remains present and the field keeps a stable automation target.
- Real-local evidence: completion used unlocked real-local Computer Use against the current `<repo>/dist/SkillsCopilot.app` bundle plus app-window screenshot evidence.

## Completed Evidence Checklist

- [x] Implemented input handling keeps committed Chinese text separate from explicit submit.
- [x] Implemented paste path preserves mixed Chinese/English task text, punctuation, line breaks, emoji, and internal spacing.
- [x] Implemented multiline task entry has stable editing, review, and submit behavior.
- [x] Implemented leading/trailing whitespace behavior is visible and consistent: whitespace-only input is not submitted, while nonblank raw text is preserved for service calls.
- [x] Implemented automation-safe input path works through Computer Use without active-input-method corruption.
- [x] Implemented explicit submit behavior prevents accidental task execution during composition, focus changes, or ordinary newline editing.
- [x] Implemented focus restoration after submit and result rendering by keeping the task text visible and the field automation target stable.
- [x] Focused Swift UI/model tests cover paste/multiline/emoji text, whitespace behavior, exact service-call text, fallback behavior, and stable accessibility identifiers.
- [x] Existing read-only service/task safety boundaries remain unchanged.
- [x] `pnpm verify:v2.75-docs` passes after the checklist is marked completed.
- [x] `pnpm check:privacy` passes without real absolute user paths in docs.
- [x] `git diff --check` passes.
- [x] Shared product gate `pnpm check:macos` passes.
- [x] Unlocked real-local Computer Use validated task input using the current `<repo>/dist/SkillsCopilot.app`.
- [x] App-window screenshot evidence was captured and verified.

## Real-local Computer Use Evidence

Validation targeted the exact current app bundle, not a stale same-bundle build:

- App bundle: `<repo>/dist/SkillsCopilot.app`
- Bundle id: `dev.skills-copilot.native`
- Process: PID `43079`
- Main window accessibility id: `skills-copilot.main-window`
- Input accessibility id: `skills-copilot.task-cockpit.input`
- Input status accessibility id: `skills-copilot.task-cockpit.input.status`
- Captured CG window id: `36527`
- Screenshot evidence: [`docs/ui-artifacts/v2.75-task-input-resilience/completed.png`](./ui-artifacts/v2.75-task-input-resilience/completed.png)

The first real-local pass caught that the SwiftUI `TextEditor` field was not AX-settable for Computer Use automation. V2.75 fixed this by using an AX-settable multiline `TextField` with the same stable input identifier. The completed Computer Use snapshot showed `skills-copilot.task-cockpit.input` as a settable string text field and `skills-copilot.task-cockpit.input.status` as `Ready for explicit submit.`

Computer Use then set this task text through the field, with visible markers showing the two leading and two trailing spaces:

```text
␠␠修复 Task Cockpit 输入 🧪
第二行 paste / 自动化输入␠␠
```

The AX read-back preserved the Chinese text, emoji, newline, and surrounding spaces. The Build action remained disabled for blank/whitespace-only task text and became enabled only after nonblank input. Clicking the visible Build action explicitly started Task Cockpit generation; no service call ran during ordinary text editing. The result rendered a bounded fallback/partial Task Cockpit response, and the task text stayed visible in the input field.

## Real-local Matrix

| Input path | Completed evidence | Result |
| --- | --- | --- |
| Chinese committed text | Computer Use set/read-back included `修复 Task Cockpit 输入` | Preserved before explicit submit |
| Paste / automation text | AX-settable multiline field accepted mixed Chinese/English text, emoji, slash text, newline, and spaces | Field and submitted task text stayed intact |
| Multiline editing | Submitted two-line task text through the visible Build action | Newline editing did not accidentally submit |
| Leading/trailing whitespace | Model tests prove whitespace-only input returns no task, while nonblank `submissionText` preserves raw text | Normalization rule is deterministic and visible |
| Automation-safe entry | Computer Use used `skills-copilot.task-cockpit.input` against `<repo>/dist/SkillsCopilot.app` PID `43079` | No active-input-method corruption |
| Focus/result stability | After explicit Build and fallback result, the same input field and result container remained visible | Editing state stayed predictable |

## Commands

```sh
swift test --package-path apps/macos --filter 'TaskInputModelTests|TaskCockpitModelTests|MainWindowModelTests|SkillStoreTests'
pnpm verify:macos-ui-layout
./script/build_and_run.sh --verify
pnpm verify:screenshot-artifacts
pnpm check:privacy
pnpm verify:v2.75-docs
pnpm check:macos
git diff --check
```

## Safety Boundary

V2.75 remains local, user-triggered, and read-only by default:

- No raw prompt persistence.
- No cloud sync.
- No provider default calls.
- No write/apply path.
- No skill file or agent config mutation.
- No script execution.
- No credential reads.
- No telemetry.
- No broad text editor rewrite.
- No new task, readiness, routing, remediation, or provider service semantics by default.

## Canonical Blockers

No canonical blocker was used for this completed closeout. If future reruns cannot complete, record exactly one primary canonical blocker and do not replace unlocked real-local Computer Use evidence with fixture smoke:

- `locked-session`
- `window-not-found`
- `no-ax-window`
- `computer-use-timeout`
- `remote-connection`
- `activation-failed`
- `black-capture`
- `flat-capture`
- `transparent-capture`
- `invalid-capture`
- `screen-recording-permission`
- `stale-bundle`
- `tool-layer-unknown`

Fixture smoke, no-capture launch success, and app-window screenshots are supporting evidence only. They cannot replace unlocked real-local Computer Use for input-method resilience.

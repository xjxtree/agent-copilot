# V2.41 Verification Checklist（文档同步）

1. Focused Rust/Swift checks：`cargo test --workspace`、`cargo clippy --workspace --all-targets --all-features`、`swift test --package-path apps/macos`。
2. `pnpm check:macos`。
3. Real local launch (`./script/build_and_run.sh run` 或 `pnpm dev:macos`) 并进行 `SkillsCopilot` 窗口 `Computer Use`/AX 观察；若窗口不可见，记录 blocker。
4. `pnpm check:privacy`。
5. App-window-only 截图后手动检查截图与本地路径/凭据占位符是否已脱敏。
6. 复核 V2.41 边界：用户触发网络请求、无后台分析、不新增写路径、不执行脚本、无 telemetry、不做 AI write-back。

## 2026-06-11 closeout record

- Focused checks passed: `cargo test -p skills-copilot-service` and `swift test --package-path apps/macos`.
- Full gate passed: `pnpm check:macos`.
- Real local app validation passed with Computer Use after launching `<repo>/dist/SkillsCopilot.app` by absolute path. Addressing Computer Use by app name first attached to a stale same-bundle-id worktree bundle, so future reruns should continue using the current bundle path explicitly.
- Real local UI check opened Settings to the AI Provider pane; no credential was entered, and Save/Test remained disabled until a valid explicit provider draft exists.
- Privacy gate passed: `pnpm check:privacy`.
- Smoke screenshot `docs/ui-artifacts/native-macos-shell/completed.png` was manually inspected and contains fixture/no-data UI only, with no real local path or credential exposure.

---
name: sample-openclaw-skill
description: Evidence sample only for an OpenClaw skill directory containing SKILL.md.
---

This fixture is derived from local OpenClaw security-scan documentation that expects skill paths to contain `SKILL.md` and reads `name:` from YAML frontmatter.

It must not be treated as a confirmed OpenClaw adapter parser contract until maintainers provide the official skill package format.

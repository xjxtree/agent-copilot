{
  "jobs": [
    {
      "id": "sample-disabled-maintenance-job",
      "description": "Evidence sample only: local hermes-ops docs say cron jobs should be disabled with enabled false instead of deleting entries.",
      "schedule": "0 3 * * *",
      "timezone": "Asia/Shanghai",
      "enabled": false
    }
  ]
}
